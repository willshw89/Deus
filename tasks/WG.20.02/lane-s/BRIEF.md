# Lane S Brief: Art Catalogue (WG.20.01 schema + WG.20.02 catalogue)

**NO ART GENERATION BY ANYONE. No image generation, no AI art, no drawing, painting or editing of sprite/tile pixels, no prompting an image model, no requesting art from anyone. This lane builds only a catalogue, blank templates or placement/validation tooling.**

NO ART (DEC-007, Owner). No AI generates art of any kind. That means no drawing, painting, generating, prompting an image model, upscaling, redrawing, recolouring or editing of art pixels, and no requesting art from anyone. Allowed by the DEC-007 amendment (a608ba1b): the art CATALOGUE (a manifest of every tile and sprite with sheet, slot, pixel coordinates, size and palette); BLANK template tilesets (empty grids and slot IDs generated from the catalogue); and PLACEMENT/validation tooling that places Owner-approved art into catalogue slots. A blank template contains nothing but grid lines, slot IDs/labels and one flat background. Tests use only synthetic solid-colour blocks generated by the test at run time. Test-harness screenshots are evidence, not art.

**Lane:** lane-s | **Task ID:** WG.20.02 (includes the WG.20.01 schema) | **Branch:** task/lane-s | **Worktree:** `C:\Users\snewt\.deus_worktrees\lane-s` | **Writer:** claude | **Reviewer:** grok (independent, later) | **Base:** main `b612bc7217349bce695e15395bd041f63673b89b` | **Source directive:** PM draft 0019-S §1, updated by PM 0028-AC §2.3 and Owner rulings of 2026-09-26 (addendum §12-§20, 01:55 CT scale ruling)

**allowedPaths** (exact; everything else is read-only):
- `art/catalogue/**`
- `docs/art/catalogue/**`
- `tools/art/build_catalogue.js`
- `tools/art/test_catalogue.js`
- `tools/art/fixtures/catalogue/**`
- `tasks/WG.20.02/lane-s/**`

**FORBIDDEN** (read only): `game/**` (including every *AssetIndex*, *WorldCatalog*, *Registry* file), `art/APPROVALS.md` (Owner only), `art/reference/**`, `art/masters/**`, `art/templates/**` (Lane T), `reference/**` (untracked, third-party: read and hash only, never copy into git), `docs/ASSET_*.md`, `docs/asset_briefs/**`, `docs/art/*.md|*.json`, every other `tools/**` path. Shared read-only helpers you should use and never edit: `tools/png_util.js`, `tools/png_read.js`, `tools/scale_resolver.js`, `tools/palette_resolver.js`. No npm dependencies.

## Goal
One deterministic, machine-readable manifest of every tile and sprite slot the game needs, sized by the scale chart and the Owner's geometry, so the Owner can later paint into blank templates (Lane T) and approved art can be placed and validated automatically (Lane U). The catalogue contains NO image data: only ids, sizes, slots, sources, statuses and references.

## Governing geometry and scale (Owner rulings; corrects every 9-layer assumption in draft 0019-S)
- World: 32 layers, default z range -16..+15 with surface at 0 (DEC-013 as amended, addendum §13). Nothing in the catalogue, templates or tools may assume 9 layers or hard-code a layer count; read it from `art/catalogue/geometry.json`.
- Sim geometry: one square = 5 ft; one layer = 10 ft; 5 strata of 2 ft per layer (addendum §12).
- Pixel geometry (Owner, 01:55 CT 2026-09-26, relayed by PM): tile = 48 px = one 5-ft square. Layer face height = 96 px (2 tiles). Stratum pixel split default = [19, 19, 19, 19, 20] px (sums to 96). The stratum split is a PARAMETER in `geometry.json` (`stratumPx`), never a literal in code: every tool reads it, and a test must show that changing it (for example to [20,19,19,19,19] or [24,24,24,24,0]-style invalid input) changes slot heights or is rejected.
- Humans stay deliberately small: about 42 px tall on the 48-px grid (the scale chart's HUMAN 42), so imposing monsters fit a 2-tile frame. Human-scale props (doors, beds, tables, chairs, fences, chests) are sized to the 42-px human per the scale chart, NOT to true feet. The scale chart (strip `art/reference/DEUS_HUMAN_SCALE_STRIP_V1.png` + its numeric source `game/data/DEUS_ScaleRegistry.json`) is the size authority for sprites and props (DEC-016 in docs/OWNER_DECISIONS.md). Sim distances govern the simulation only.
- Character-size authority (PM 01:59 CT, derived from SRD 5.1 text in `game/data/srd51/character_options.json` race "Size" traits and the `game/data/srd51/rules.json` Size Categories table): creatures are drawn at 7 px per foot (a 6-ft human = 42 px) on the 48-px, 5-ft grid. The sim keeps the true SRD heights; these are drawn heights only.
  - Race drawn heights (all in 48x48 frames): Dragonborn 46-48 px; Half-Orc, Human and Tiefling 35-46; Elf 33-44; Half-Elf 35-42; Dwarf 28-35; Gnome 21-28; Halfling about 21. Undecided (Owner): a readability floor of 26-28 px for Gnome and Halfling with slightly larger heads is a parameter with default OFF (see below), never applied silently.
  - Sprite frame vs footprint (Owner correction, 02:04 CT): the SRD space (e.g. Large = 2x2 squares) is the combat/footprint area in the sim and is stored SEPARATELY from the sprite frame. Every creature/character entry carries both `footprint` (squares, from SRD size) and `frame` (pixels, from the frame class below). Never derive one from the other.
  - Frame classes: `TINY` = 24x24 footprint drawn in a 48x48 frame; `SMALL` and `MEDIUM` = 48x48 frame (1 tile); `LARGE_TALL` (tall-bodied Large: humanoids such as ogres and trolls) = 48 wide x 96 tall frame on a 2x2 footprint (Owner); `LARGE_LONG` (long-bodied Large: horses, big beasts) = 96 wide x 48 tall frame on a 2x2 footprint (PM proposal consistent with the Owner's call; `status: PROPOSED`). Large frames are NOT 96x96. `HUGE` (3x3 footprint) and `GARGANTUAN` (4x4+ footprint) follow the same tall/long principle but their frame sizes are TBD by the Owner: store them as parameters (`frame: null`, `status: OWNER_OPEN`, `future: true`) and generate no paint slots for them until set.
  - Undecided parameters, default OFF: `TALL_MEDIUM` (a 48x64 frame for Medium creatures up to 8 ft) and the Gnome/Halfling readability floor (26-28 px with slightly larger heads). Both exist in geometry/size data as parameters with `enabled: false`; nothing uses them unless the Owner turns them on, and a test shows that turning one on changes the affected slots.
  - RMMZ character sheet blocks are 3x4 frames: 48x48 -> 144x192; 48x96 (LARGE_TALL) -> 144x384; 96x48 (LARGE_LONG) -> 288x192. Larger blocks (e.g. 288x384, 432x576, 576x768) only once the Owner sets Huge/Gargantuan frames.
- Biome bands (PM defaults, OPEN for the Owner): LOWER2 (deep) -16..-9, LOWER1 (shallow underground) -8..-1, SURFACE 0..+3, UPPER1 +4..+9, UPPER2 +10..+15. 25 biomes = 5 bands x 5 biomes. The biome-to-band assignment and the 25 biome names are OWNER_OPEN: never invent names. `game/data/DEUS_BiomeRegistry.json` lists 5 canonical biomes (TEMP, WET, ARID, HIGH, VOLC) and forbids snow/ice; record the 5-vs-25 gap in conflicts, and use neutral placeholder ids `<BAND>_B1..B5` (flag `ownerOpen: true`) where a per-biome slot is needed before the Owner rules.

## Catalogue contract `deus-art-catalogue/1.1.0` (frozen for lanes S, T and U)
Lane S owns the schema; lanes T and U build against this contract using private fixtures. Lane S may ADD optional fields but must not rename, remove or change the meaning of any field below. If S needs to, it writes escalation.md instead.
- `art/catalogue/geometry.json`: `{schemaVersion, squareFt: 5, layerFt: 10, strataPerLayer: 5, stratumFt: 2, layerCount: 32, zMin: -16, zMax: 15, tilePx: 48, layerPx: 96, stratumPx: [19,19,19,19,20], humanPx: 42, frameClasses: {TINY:{footprintSq:[1,1], drawnFootprintPx:[24,24], frame:[48,48]}, SMALL:{footprintSq:[1,1], frame:[48,48]}, MEDIUM:{footprintSq:[1,1], frame:[48,48]}, LARGE_TALL:{footprintSq:[2,2], frame:[48,96]}, LARGE_LONG:{footprintSq:[2,2], frame:[96,48], status:"PROPOSED"}, HUGE:{footprintSq:[3,3], frame:null, status:"OWNER_OPEN", future:true}, GARGANTUAN:{footprintSq:[4,4], frame:null, status:"OWNER_OPEN", future:true}}, optionalParams: {TALL_MEDIUM:{frame:[48,64], enabled:false}, smallRaceReadabilityFloorPx:{value:[26,28], races:["GNOME","HALFLING"], enabled:false}}, pxPerFootCreature: 7, rmmzCharacterBlocks: {"48x48":[144,192], "48x96":[144,384], "96x48":[288,192]}, bands: [{id, zMin, zMax, ownerOpen: true}], decisions: [{field, source, status}]}`.
- `catalogue.json` top level: `schemaVersion`, `tileSizePx: 48`, `geometry {path, sha256}`, `palette {path, sha256}` (art/palette/deus_master_world_palette_v1.hex), `scaleChart {path, sha256}`, `sources[] {path, sha256, role}`, `sheets[]`, `entries[]`, `outOfScope[] {sourceId, reason}`.
- `sheets[]`: `{sheetId, kind: ATLAS|RMMZ_TILESET|RMMZ_CHARACTER, group {band, biome, type}, w, h, gridPx, runtimeFile|null}`. ATLAS sides are multiples of 48 and at most 4096. RMMZ sheets use the fixed sizes in docs/RMMZ_ASSET_SPEC.md (A1/A2 768x576, A3 768x384, A4 768x720, A5 384x768, B-E 768x768; `$` character sheets 3 x frameW by 4 x frameH).
- `entries[]`: `id` (`BAND_BIOME_CATEGORY_TYPE_VARIANT_STATE`), `category`, `band`, `biome`, `zMin`, `zMax` (inside geometry range), `sizeClass` (SRD size) and `frameClass` (for characters/creatures; footprint and frame stored separately), `sourceIds {catalog[], assetIndex[], brief[], ar[], manifest[], matrix[], addendum[]}`, `scaleRow` (required; a scale_chart.json row id, or a geometry row id such as `GEOM_TILE`, `GEOM_STRATUM_1..5`, `GEOM_LAYER_FACE`, `GEOM_FRAME_LARGE_TALL`, or a `size_classes.json` race row such as `RACE_HUMAN`), `envelope {wMin,wTarget,wMax,hMin,hTarget,hMax}`, `footprint {w,h}` (tiles), `anchor {type: GROUND|CEILING|WALL|CENTER, x, y}`, `paletteRampIds[]`, `frames {cols, rows, facings, rate}`, `slot {sheetId, slotId, x, y, w, h} | null` (slotId = `<sheetId>:<4-digit index>`), `runtime {kind, file, index|tileId}`, `variants {derivedFrom|null, flipH, flipV, rot, paletteSwap|null, lightingSafe}`, `paperDoll {...}|null`, `references[]`, `standardPending|null`, `alphaMode: BINARY|OWNER_OPEN`, `status: MISSING|EXISTING_UNAPPROVED|STAND_IN|STOCK|APPROVED|OUT_OF_SCOPE`, `statusWhy`.
- Slot sizing rule: slot w = 48*ceil(wMax/48), h = 48*ceil(hMax/48) per frame; geometry-derived pieces (edge/cliff strips, wall faces, ramps) take their heights from `stratumPx`/`layerPx`, never from literals. Variant rows (`derivedFrom` set) own no paint slot (`slot: null`).

## Deliverables
1. `art/catalogue/catalogue.schema.json` (JSON Schema 2020-12, `$id` `deus-art-catalogue/1.1.0`) implementing the contract above, plus `docs/art/catalogue/SCHEMA.md` (field-by-field, with the geometry and size-class rules).
2. `art/catalogue/geometry.json` exactly as in the contract, with a `decisions[]` list citing the source of each value (addendum §12/§13, Owner 01:55 CT) and its status (DECIDED or OPEN).
3a. `art/catalogue/size_classes.json`: the machine-readable character-size authority from the contract: one row per SRD race (id `RACE_<NAME>`, `srdSize`, `srdSourceRef` (file + entry id + quoted Size trait text), `drawnHeightPxMin`, `drawnHeightPxMax`, `frameClass`, `status: DERIVED|PROPOSED`, `readabilityFloorPx` for Gnome and Halfling as a parameter with `enabled: false`) and one row per frame class (TINY, SMALL, MEDIUM, LARGE_TALL, LARGE_LONG, HUGE, GARGANTUAN) with footprint (squares), frame (px or null), RMMZ block size and status, plus the optional parameters TALL_MEDIUM and the small-race readability floor with `enabled: false`. Build it from the SRD files and check the numbers against them (7 px/ft); a mismatch between the SRD text and the numbers in this brief goes into conflicts.md, never silently fixed. Character/creature entries cite a `RACE_*` or frame-class row.
3. `art/catalogue/scale_chart.json`: one row per `game/data/DEUS_ScaleRegistry.json` class (39), keyed by class id, carrying the registry's W/H min/target/max, footprint and anchor. Where the strip labels the object, also `chartLabel` and `chartHeightPx`, transcribed by reading the strip image labels (18 labelled objects, e.g. HUMAN 42, SHORT GRASS 12, TALL GRASS 28, SMALL BUSH 18, MED BUSH 26, MED ROCK 22, BOULDER 38, DOORWAY 58, BED 58, CHAIR 26, TABLE 24, CHEST 20, OAK 84, BIRCH 88, PINE 92, ACCENT 105, HERO 140, plus 48x48 TILE 48). Each row carries `source: STRIP+REGISTRY|REGISTRY_ONLY`. Add tile-class rows (48x48 ground tile; A1/A2 autotile block 96x144; A3 96x96; A4 96x120) with `source: RMMZ_SPEC`, and geometry rows (`GEOM_*`) with `source: GEOMETRY` computed from geometry.json. Never invent other rows.
4. `tools/art/build_catalogue.js`: deterministic (no timestamps, stable key order, stable sort). It regenerates `art/catalogue/catalogue.json`, `art/catalogue/conflicts.md`, `art/catalogue/references.json` and `docs/art/catalogue/*.md` (one page per band plus INDEX.md with counts) from these sources: docs/ASSET_MANIFEST.md; game/data/srd51/character_options.json and game/data/srd51/rules.json (race Size traits and the Size Categories table, for size_classes.json); game/data/UF_AssetIndex.json (the runtime copy) and game/data/DEUS_AssetIndex.json (stale fork; conflicts only); game/data/UF_WorldCatalog.json; docs/asset_briefs/SEG-00..18 (ids, deliver size, anchor and category only; ignore generation text); docs/ASSET_REQUESTS.md (AR rows); docs/ASSET_INVENTORY.md (status); docs/art/DEUS_BIOME_PRODUCTION_MATRIX.json; game/data/DEUS_{Scale,Palette,Biome}Registry.json; docs/RMMZ_ASSET_SPEC.md; art/catalogue/geometry.json; and the Owner addendum families below. `--check` rebuilds in memory and exits non-zero if any committed output differs. The build FAILS on: a duplicate id or slotId; a missing size, envelope, footprint, anchor or palette ramp; a ramp id not in the palette registry; a `scaleRow` not in scale_chart.json; a size outside its chart row's min/max; a slot smaller than the chart envelope; overlapping slot rects in a sheet; a slot off the 48 grid or outside its sheet; a sheet over 4096 or not a multiple of 48; a `zMin/zMax` outside geometry range; an entry that traces to no source (and is not outOfScope with a reason); a source id dropped without an outOfScope reason; a `derivedFrom` pointing at a missing or derived base; a variant with a paint slot; a paper-doll part whose frame layout or anchor differs from its bodyType base; a geometry-derived slot height that is not computed from `stratumPx`/`layerPx`. Packing: deterministic shelf packing by (band, biome, type, category, id); atlases up to 4096x4096.
5. Owner addendum families (entries/slots only, status MISSING, NO pixels, each tagged with `sourceIds.addendum`):
   - Edge and cliff-face strips for every stratum height difference 1..5 (and the full-layer face) per terrain and facing, plus height-shading overlays (§15). One top-surface tile per terrain; NOT a full tile set per height.
   - Ramp and slope pieces: 5 cells per layer (one stratum per cell), per terrain x rise direction N/E/S/W, plus ramp side faces and stair/ladder connector pieces (§16).
   - Decay-stage overlay slots per building material: WEATHERED, OVERGROWN, COLLAPSED, BURIED; overlays preferred over full tiles (§7).
   - Wall faces: visible inner side walls of openings, per height difference (§19 cue 1).
   - Rim shadows cast onto lower layers, `standardPending: "DW.01.06"`, `alphaMode: OWNER_OPEN` (binary until the Owner rules) (§19 cue 2).
   - Per-depth palettes: placeholders `DEPTH_<band>` as derived variants with no paint slot and NO colour values (§19 cue 3). Never invent palette colours.
   - Hanging props with a CEILING anchor: roots, vines, stalactites, waterfalls, dust, light shafts (§19 cue 5); and deep light sources (§19 cue 6).
   - Character/creature frame slots by frame class (TINY in 48x48, SMALL/MEDIUM 48x48, LARGE_TALL 48x96, LARGE_LONG 96x48 PROPOSED; HUGE/GARGANTUAN as OWNER_OPEN parameters with no paint slots; TALL_MEDIUM off by default), laid out in RMMZ 3x4-frame blocks (144x192, 144x384, 288x192). Footprint (SRD squares) and frame (px) are separate fields.
   - Organisation by biome band: every band has its sheets (band x biome x type, `SHARED` for neutral content), and every addition family appears in every band where it is physically possible (write the reason where it is not, e.g. no rain-fed waterfall in a sealed deep band if a source says so).
   - Terrain list: take it from the registries/sources and cite the file; never invent terrains. Record each terrain list you used in SCHEMA.md.
6. `art/catalogue/conflicts.md` (generated): every disagreement between sources, file:line on both sides, NO resolution. At least: UF_ vs DEUS_ AssetIndex (entries only in one; AR-200 vs AR-2000 style ref drift); biomes (matrix 6 incl. "Cold" vs registry 5 canonical forbidding snow/ice; 5 canonical vs 25 biomes in 5 bands); scale disagreements with the chart (DEUS_WORLD_ART_VISUAL_CHARTER.md §3 grass/bush sizes; DEUS_SCALE_AND_ASSET_MASTER_BIBLE.md door 64-80 vs chart 58; asset_briefs/INDEX.md "every asset inside one 48x48" vs trees 84-140; docs/design/SCALE.md person 44-48; APPROVALS.md human anchors 46 px vs chart 42/44; TREE_SAPLING in registry but not in the standard); the geometry-vs-chart px/ft difference (48 px per 5 ft = 9.6 px/ft vs a 42-px human) recorded as the Owner's deliberate choice (01:55 CT), not as an error; palette (approved art on uf.hex 256 vs master 226); ASSET_MANIFEST 18 rows vs 20 claimed; DW.01.06 autotile standard pending; 4096 atlases vs fixed RMMZ sizes (atlas = paint sheet, runtime = export); flips/rotations vs upper-left lighting (`lightingSafe` false by default); every 9-layer assumption found in any source.
7. Coverage: 100% of the WorldCatalog ids, the brief anchor/ui/eq/face ids, the manifest rows and every open AR row map to an entry or to outOfScope with a reason; every addendum family is present for every band. Print the coverage table in docs/art/catalogue/INDEX.md.
8. References: `art/catalogue/references.json`, per entry and per pack: `references[] {path, sha256, kind: PERSPECTIVE|BIOME|SCALE|PALETTE|STYLE_ANCHOR|POSE, tracked, ownerApproved: YES|NO|UNKNOWN, thirdParty, styleAnchorEligible}`. Link by path + sha256 only; never copy a reference into git. Tracked references under art/reference/* must hash-match or the build fails. Untracked `reference/**` is Ultima VII-derived third-party material: `thirdParty: true, styleAnchorEligible: false`, `UNVERIFIED_ABSENT` (warning) when missing. Style-lock refs carry `ownerApproved: UNKNOWN` until the Owner confirms.
9. `tasks/WG.20.02/lane-s/REPORT.md`: commands run with raw EXIT lines, entry count, sheet count and largest atlas, coverage %, outOfScope count, conflicts.md line count, the Owner questions (copied, not answered), and the final `git rev-parse HEAD`.

## Done tests (`tools/art/test_catalogue.js`, exits non-zero on failure)
Each check has a provocation `UF_TEST_PROVOKE=catalogue.<check>` shown to fail it (record the provoked runs in REPORT.md):
- the schema validates catalogue.json (write a small in-file validator for the subset of JSON Schema you use; no npm deps)
- the rebuild is byte-identical twice (sha256 equal)
- one check per FAIL rule in deliverable 4, each against a fixture under tools/art/fixtures/catalogue/
- scale_chart.json vs DEUS_ScaleRegistry.json: every STRIP row's chartHeightPx equals the registry's visual height target; disagreements are listed in conflicts.md, never "fixed"
- geometry: stratumPx sums to layerPx; changing stratumPx in a fixture changes the generated strip/wall/ramp slot heights; no 9 or 32 literal layer count in build_catalogue.js outside geometry reading (grep check)
- size classes: size_classes.json matches the SRD Size traits and the 7 px/ft rule (6-ft human = 42 px); footprint and frame are separate fields on every creature entry; TINY draws in 48x48; LARGE_TALL frames are 48x96 and LARGE_LONG 96x48 (no 96x96 Large frame exists); HUGE/GARGANTUAN frames are null parameters with no paint slots; TALL_MEDIUM and the Gnome/Halfling floor are `enabled: false` and enabling one in a fixture changes the affected slots; character blocks are 3x4 frames (144x192, 144x384, 288x192)
- coverage = 100% including every addition family x band
- references hash-verify
- an atlas side over 4096 is rejected; a variant row owns no paint slot; DEPTH_<band> rows carry no colour values
Gate commands (the merge gate runs these): `node tools/art/test_catalogue.js` and `node tools/art/build_catalogue.js --check`, both exit 0.

## Acceptance criteria (the Grok reviewer will check these)
- Both gate commands exit 0 in a fresh clone; the provocations each fail.
- No file outside allowedPaths changed (`git diff --name-only b612bc7217349bce695e15395bd041f63673b89b..HEAD`).
- No image data anywhere in the catalogue; no PNG committed by this lane.
- conflicts.md resolves nothing; Owner questions are listed, not answered.
- Every geometry-derived size traces to geometry.json; no 9-layer assumption remains.

Commit messages start `[claude] WG.20.02`.

## Standing rules (verbatim, every lane)
1. One primary writer per file set. Your write set is exactly the allowedPaths above. It is disjoint from every other live lane: K (`game/js/plugins/DEUS_Depth.js`, `game/js/plugins.js` DEUS_Depth block, `DEUS_Minimap.js`, `DEUS_Fog.js`, `DEUS_DayNight.js`, `docs/systems/DEUS_Depth.md`, `docs/systems/DEUS_Minimap.md`, `tools/test_layer_render_flat.js`, `tools/bench_render_layers.js`, `tools/test_minimap.js`, `tasks/WG.00.09b/lane-k/**`), N (`game/js/plugins/DEUS_Levels.js`, `game/js/plugins/DEUS_World.js`, `tools/test_layer_switch_inplace.js`, `tasks/SIM.00.00/lane-n/**`), M (`docs/adr/ADR-003*`, `docs/adr/README.md`, `tasks/SIM.00.01/**`), E (paused), the merged governance/ops tools (`tools/governance/**`, `tools/ops/**`: read only), the coordinator's files (`docs/STATUS.md`, `docs/VISION.md`, `docs/OWNER_DECISIONS.md`, every `*WBS*.md`, `docs/telemetry/**`: read only) and the other new lanes listed in the table at the end of this brief.
2. Capture the exit code of every command, one per command (`"EXIT=$LASTEXITCODE"` in PowerShell, `echo EXIT=$?` in bash). Put raw values in your reports. Never type or paraphrase a commit hash: paste it from `git rev-parse` output.
3. Run tests in the FOREGROUND. Never end your turn while background jobs or child processes are running. Commit early (WIP commits allowed on your branch). Do not merge. Write only inside allowedPaths.
4. Commit and push to your own branch only: `git push origin <your branch>` after your final commit (never any other branch, never `main`, never force). If the push is refused, do not work around it (never set `DEUS_INTEGRATOR`); write the refusal in your REPORT and stop; the PM pushes.
5. Do not merge. Do not self-certify: never write DONE, PASS, VERIFIED or CLOSED about your own work in any file. Your REPORT states what you did and the raw evidence. An independent Grok review decides; the PM merges.
6. Stop and write `tasks/<task>/<lane>/escalation.md` (then commit it) if you need a file outside your allowedPaths, find a bug in a read-only shared file, or find two sources that disagree in a way the brief does not settle. Never resolve an Owner question yourself.

## New lanes opened together (write sets are disjoint; do not touch another lane's paths)
| Lane | Task | Role | Write set |
|---|---|---|---|
| S | WG.20.02 art catalogue | Claude writer | `art/catalogue/**`, `docs/art/catalogue/**`, `tools/art/build_catalogue.js`, `tools/art/test_catalogue.js`, `tools/art/fixtures/catalogue/**`, `tasks/WG.20.02/lane-s/**` |
| T | WG.32.02 blank templates | Claude writer | `tools/art/make_blank_templates.js`, `tools/art/test_blank_templates.js`, `tools/art/fixtures/templates/**`, `art/templates/**`, `tasks/WG.32.02/lane-t/**` |
| U | WG.41.01 placement + validator | Claude writer | `tools/art/place_art.js`, `tools/art/validate_art.js`, `tools/art/test_place_art.js`, `tools/art/fixtures/place/**`, `docs/art/APPROVALS_FORMAT.md`, `tasks/WG.41.01/lane-u/**` |
| gap-audit | SIM.50.01 living-world gap audit | Claude writer (coordinator-launched) | `docs/audits/LIVING_WORLD_GAP_AUDIT.md`, `tasks/SIM.50.01/gap-audit/**` |
| P | SIM.60.01 SRD spell-effect audit | Claude writer | `docs/audits/SRD_SPELL_EFFECT_AUDIT.md`, `docs/audits/srd_spell_effect_audit.json`, `tasks/SIM.60.01/lane-p/**` |
| M | SIM.00.01 ADR-003 Rev 3 | Claude writer | `docs/adr/ADR-003_sim_render_split_and_lod.md`, `docs/adr/README.md`, `tasks/SIM.00.01/lane-m/**` (not review files) |
| N review | SIM.00.00 in-place switch | Grok reviewer | `tasks/SIM.00.00/lane-n/review_grok_2f2a1ff2.md` only |
