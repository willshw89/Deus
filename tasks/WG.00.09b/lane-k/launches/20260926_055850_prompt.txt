=== FIX 2 === Lane K (WG.00.09b) Fix 2: read tasks/WG.00.09b/lane-k/BRIEF_FIX2.md FIRST. It overrides BRIEF_FIX1.md and BRIEF.md where they differ.
You are the primary writer (Claude) for lane-k (Task WG.00.09b: flat 1:1 layer depth rendering per DEC-011, and layer-switch lag fixes), Fix 2. Your Fix1 tip abdb6bbe got an independent Grok VERDICT: CLEAN PASS (review_grok_abdb6bbe.md), but after merging into main the gate test layers_flat.switch_same_frame FAILS on every run (11/12): main now contains Lane N's in-place level switch (SIM.00.00, merge e27e8be5), and at levels:viewChanged the unit sprites on the planes' levels have no frame yet. The PM has already merged current main (1f683b94) into task/lane-k (00ff1c59) and committed BRIEF_FIX2.md (d137fe13); start from that tip.
Read tasks/WG.00.09b/lane-k/BRIEF_FIX2.md carefully and follow every instruction in it. Also read BRIEF_FIX1.md, BRIEF.md, review_grok_abdb6bbe.md, and Lane N's code in game/js/plugins/DEUS_Levels.js (finishSwitch, the Spriteset_Map.update alias) and DEUS_World.js (switchViewInPlace, rebindSpriteset) - read-only for you.
Key rules (from the briefs):
1. NO ART GENERATION BY ANYONE (DEC-007). Never generate, draw, edit, request or integrate art. Harness screenshots are evidence only.
2. Write only inside the allowedPaths of tasks/WG.00.09b/lane-k/lane.json. Never edit lane.json or review_grok_*.md. Never edit Lane N's files (DEUS_Levels.js, DEUS_World.js, tools/test_layer_switch_inplace.js). If a fix needs a forbidden file, stop that item and write the exact need to tasks/WG.00.09b/lane-k/escalation.md.
3. Do not weaken layers_flat.switch_same_frame: it keeps judging the state at levels:viewChanged. Owner rule DEC-011: all layers 1:1, no filters, and no lag in other-layer sprites after a layer change. A known failure is not acceptable.
4. Gates must be deterministic. Required on the final tip, each in a fresh temp clone: layers_flat 12/12 on 3 consecutive runs plus --provoke once; --suite depth 27/27; tools/test_minimap.js; tools/test_layer_switch_inplace.js 7/7; tools/check_deus_syntax.js; tools/test_palette.js; bench normal x2 with machineLoad. Never kill or pause another process to get a quiet machine.
5. Run heavy NW.js suites and the benchmark in fresh temp clones of your committed tip, not in the live worktree; delete the clones afterwards.
6. Run commands in the FOREGROUND. Never end your turn while background jobs or child processes are running. Commit early (WIP commits allowed on your branch). Capture every exit code and paste raw output in REPORT.md (new "Fix 2" section).
7. Do not merge into main. Do not self-certify; an independent Grok review (launched later by the PM) decides.
8. When finished, commit and then push your own branch: git push origin task/lane-k. Never push any other branch, never main, never force, never set DEUS_INTEGRATOR. If the push is refused, say so in REPORT.md and stop.
9. Your final output line must be exactly: FINAL SHA: <sha> (pasted from git rev-parse HEAD after the push).
Commit messages start with '[claude] WG.00.09b Fix2'.