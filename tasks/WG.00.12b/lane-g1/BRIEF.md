# Lane G1 Brief: merge-gate / check_claims / launcher trust fix for PM-opened lanes (WG.00.12b)

**NO ART GENERATION BY ANYONE (DEC-007).** Tooling lane: governance and ops scripts and their tests only.

**Lane:** lane-g1 | **Task ID:** WG.00.12b (follow-up sub-package of WG.00.12 Consolidation, per WBS rule 4 `<leaf>a/b/c` naming, `docs/worldgen/DEUS_WORLDGEN_WBS.md:387`; it follows Lane I merge gate `099b6878`, Lane J launcher `62067d1c`, Lane C2b checker `29d60a96`; `docs/STATUS.md` "Open Ops Debt (PM)" lists it) | **Branch:** task/lane-g1 | **Worktree:** `C:\Users\snewt\.deus_worktrees\lane-g1` | **Writer:** claude | **Reviewer:** grok (independent attack/mutation review, launched later by the PM; not DONE until it passes) | **Base:** origin/main `9cba41eaf6378048d50004dcf243defbfe7f17f4` | **Source:** Owner-authorised main-chat ops 2026-09-26.

**allowedPaths** (exact; mirrored in `tasks/WG.00.12b/lane-g1/lane.json`):
- `tools/governance/merge_gate.js`, `tools/governance/test_merge_gate.js`, `tools/governance/MERGE_GATE.md`
- `tools/governance/check_claims.js`, `tools/governance/test_check_claims.js`, `tools/governance/README.md`
- `tools/ops/launch_worker.ps1`, `tools/ops/test_launch_worker.ps1`, `tools/ops/resume_queue.ps1`, `tools/ops/test_resume_queue.ps1`, `tools/ops/README.md`
- `tasks/WG.00.12b/**` (REPORT and evidence; the launcher also writes its saved prompt under `tasks/WG.00.12b/lane-g1/launches/`; leave it alone)

**FORBIDDEN:** everything else, including `tools/ops/hooks/**`, `tools/ops/install_lane_hooks.ps1`, `tools/ops/gate_tests.json`, `tools/governance/check_wbs_integrity.js`, `game/**`, `docs/**` (read only, including STATUS, OWNER_DECISIONS and every `*WBS*.md`), `art/**`, and every file outside the repo (the live registry `C:\Users\snewt\.deus_worktrees\logs\registry\active_workers.json`, `C:\Users\snewt\.deus_pm\**`, `C:\Users\snewt\.deus_worktrees\logs\**`). **Never launch a real AI worker, never run `launch_worker.ps1` or `resume_queue.ps1` against the real registry or real lanes, and never kill a process your tests did not start** (live workers are running on this machine). Use the suites' own fixtures/temp registries only. You do NOT mint WBS IDs or change statuses; proposed follow-ups are `PROPOSED-G1-NN`.

## Background (why)
- **Authority change (Owner, 2026-09-26 ~01:50 CT; directive 0028-AC A0):** the PM (Grok Bot, main chat) now opens lanes, launches workers and merges. The PM opens each lane with a `[pm]` commit carrying `tasks/<id>/<lane>/BRIEF.md` and `lane.json` (examples: `git show --stat d9766aa2` on `task/lane-r`, `147bf517` on `task/lane-s`), and registers write-set claims on main with `[pm]` commits to `docs/STATUS.md` (example `f5c1dfd2`).
- **Merge gate:** `tools/governance/merge_gate.js` check (a) MANIFEST trusts `lane.json` only if every commit that changed it is a non-merge `[gemini]` commit (header comment ~line 12; `checkManifest` ~lines 441-470, `family(h.tag) !== "gemini"`). Every PM-opened lane is therefore refused `MANIFEST_TAMPERED` and scope/review/tests are skipped. Raw example (Lane S, 04:16 CT): `REFUSED MANIFEST_TAMPERED: 147bf517... "[pm] Open lane-s (WG.20.02): BRIEF.md and lane.json" changed tasks/WG.20.02/lane-s/lane.json; only [gemini] commits may change it` / `GATE: REFUSED (exit 1)` (the PM then merged manually). MANIFEST_INVALID also appears on some PM manifests; find out whether that is a real manifest defect or the gate being too strict, and report which.
- **check_claims:** `tools/governance/check_claims.js` `AGENT_ALIASES` (~line 126) has gemini, claude/fable, grok (with aliases `grok_pm`, `grok_bot`), codex, owner, but no `pm`. So rule 4.4 (single-writer whitelist, ~line 1253) fails on `[pm]` brief commits and `[pm]` STATUS claim commits.
- **Launcher:** `tools/ops/launch_worker.ps1` `New-DeusLanePrompt` (~line 860) injects standing rule 2 "... Do not push. Do not merge ..." whenever no `-PromptFile` is given, contradicting briefs that require `git push origin task/<lane>` and a `FINAL SHA:` line. `tools/ops/resume_queue.ps1` (~lines 194-205) relaunches QUEUED lanes with `-BriefPath` and `-ResumeFromSha` but **no `-PromptFile`**, so every resume gets that wrong default prompt. The launcher already saves each prompt as `tasks/<task>/<lane>/launches/<stamp>_prompt.txt` and records `launchPromptPath` in the registry entry (~lines 983-1015).

## What to change
1. **merge_gate.js: trust `[pm]` manifest commits.** A `lane.json` is trusted when every commit that changed it is a non-merge commit tagged `[gemini]` **or** `[pm]`. Keep `MANIFEST_TAMPERED` for everything else (a `[claude]`, `[grok]`, `[ops]`, `[codex]`, untagged or merge commit that changes the manifest must still stop the gate before scope/review/tests, exactly as now). `[pm]` is trusted for manifest provenance only: it must NOT count as a writer or reviewer family in check (b) REVIEW (a `[pm]` commit is never a review commit), and must not weaken any other check. Keep the existing mutation switch (`manifest_provenance_off`) meaningful and add mutants for the new rule (for example "trust every tag", "trust [pm] merge commits", "stop trusting [gemini]"), each killed by the suite. Update the header comment and `MERGE_GATE.md`.
2. **check_claims.js: add a `pm` agent.** `pm` becomes a known agent (its own canonical agent, not an alias of grok; the existing `grok_pm` -> grok alias stays unless you find it breaks something, in which case report it and do not change it silently). Give `pm` a documented 4.4 whitelist matching what the PM actually writes: lane-opening files `tasks/<id>/<lane>/BRIEF*.md` and `tasks/<id>/<lane>/lane.json`, and claim rows in `docs/STATUS.md`; derive where the whitelist lives the same way the checker does for other agents (read how 4.4 finds a committer's whitelist) and document it. `pm` must not be able to self-certify (rule 4.2) or act as a closer/reviewer. Show with fixtures that a `[pm]` lane-opening commit and a `[pm]` STATUS claim commit now pass 4.4, and that a `[pm]` commit touching code (for example `game/js/plugins/X.js` or `tools/governance/merge_gate.js`) still fails. Run `node tools/governance/check_claims.js --range f5c1dfd2~1..f5c1dfd2` and on the Lane R brief commit (`--range d9766aa2~1..d9766aa2`, from a checkout where it exists) before and after your change and paste both outputs.
3. **launch_worker.ps1: never inject "Do not push" when the brief says to push.** When no `-PromptFile` is passed: (a) if the lane has a saved **writer** prompt (the latest committed `tasks/<task>/<lane>/launches/*_prompt.txt` from a writer launch, or the `promptFile`/`launchPromptPath` of the lane's last writer registry entry), reuse it (with the resume line prepended when `-ResumeFromSha` is set, and without stacking a second resume line if the saved prompt already starts with one); careful: reviewer launches with `-NoCommitPrompt` also write prompt files into the same `launches/` folder, so a reviewer prompt must never be reused for a writer and vice versa; (b) otherwise generate the default prompt, but when the brief or `lane.json` says to push (for example the brief contains `git push origin <the lane branch>`, or an optional `lane.json` field you define and document such as `"push": true`, which merge_gate's `validateManifest` must then accept), rule 2 must say to commit and push the lane's own branch only (never main, never force, never `DEUS_INTEGRATOR`) and the prompt must end with the `FINAL SHA: <sha>` instruction; keep "Do not push" only for lanes whose brief does not ask for a push. Record the source prompt path (`promptFile`) in the registry entry.
4. **resume_queue.ps1:** pass `-PromptFile` for each relaunch using the lane's saved writer prompt (registry `promptFile` if it still exists, else the rule in 3a); fall back to the launcher default only when none exists, and log which one was used.
5. **Tests:** update and extend the suites. All existing checks must still pass (baselines at base: `test_merge_gate.js` RESULT 89 passed; `test_launch_worker.ps1` 147 checks; `test_resume_queue.ps1` 68 checks; `test_check_claims.js` 254 passed). Add checks for each new behaviour with a mutant that must fail. Report before/after counts.
6. **Docs:** `tools/governance/MERGE_GATE.md`, `tools/governance/README.md` and `tools/ops/README.md` describe the new trust rule, the `pm` agent and whitelist, and the prompt-selection rules, with the authority change cited (0028-AC A0, Owner 01:50 CT 2026-09-26).

## Tests and commands (heavy suites in a temp clone only)
- The four suites spawn git clones and processes. Run them **only in a throwaway clone** of your own committed branch under `%TEMP%` (for example `git clone <worktree path> $env:TEMP\g1_verify_<stamp>`, then `git -C ... checkout <sha>`), in the FOREGROUND, one per command with `EXIT=` captured, then delete the clone. Commit (WIP is fine) before each verification clone so the evidence names a real sha.
- Commands: `node tools/governance/test_merge_gate.js`, `node tools/governance/test_check_claims.js`, `powershell -NoProfile -ExecutionPolicy Bypass -File tools/ops/test_launch_worker.ps1`, `powershell -NoProfile -ExecutionPolicy Bypass -File tools/ops/test_resume_queue.ps1`, `node tools/check_deus_syntax.js`.
- Also dry-run the new gate (no merge) against the real Lane R manifest history from the temp clone if the gate has a dry-run/report mode, and show the manifest check now passes for a `[pm]`-opened lane while a fixture with a `[claude]` manifest edit is still refused. Never let the gate merge or push anything real.
- Before your final commit, run every `gateTests` entry of `tasks/WG.00.12b/lane-g1/lane.json` exactly as written (in the temp clone) and paste raw output with `EXIT=` lines in REPORT.md. Paste `git diff --name-only 9cba41eaf6378048d50004dcf243defbfe7f17f4..HEAD` and show every path is in allowedPaths.

## Deliverables
1. The tool, test and doc changes above.
2. `tasks/WG.00.12b/lane-g1/REPORT.md`: each change with `file:line`; every command with raw `EXIT=`; suite before/after counts and mutants killed; the check_claims before/after outputs; the MANIFEST_INVALID finding; the scope diff; PROPOSED-G1-NN follow-ups (for example: the `tools/ops/hooks/pre-push` message "Lane worktrees do not push" now contradicts the push policy; the gate still judges itself with main's copy, so this lane's own merge will be refused by the old gate and must be a PM manual merge); open questions for the PM/Owner; final `git rev-parse HEAD`.

## Acceptance criteria (the independent Grok reviewer will check these)
- `[pm]` and `[gemini]` non-merge manifest commits are trusted; any other manifest change is still `MANIFEST_TAMPERED` and stops the gate; `[pm]` never satisfies the review check; mutants for each clause are killed.
- `pm` is a known check_claims agent with a narrow, documented 4.4 whitelist; `[pm]` code edits still fail; `pm` cannot self-certify.
- Launcher and resume_queue reuse the lane's saved writer prompt, never mix reviewer and writer prompts, never stack resume lines, and never inject "Do not push" for a brief that says to push; tests prove each.
- All four existing suites pass with counts at or above baseline, run in a temp clone; `check_deus_syntax` exits 0.
- No file outside allowedPaths changed; no real worker launched or killed; nothing self-certified.

Commit messages start `[claude] WG.00.12b`.

## Standing rules (verbatim, every lane)
1. One primary writer per file set. Your write set is exactly the allowedPaths above. It is disjoint from every other live lane (table below) and the coordinator's files (`docs/STATUS.md`, `docs/VISION.md`, `docs/OWNER_DECISIONS.md`, every `*WBS*.md`, `docs/telemetry/**`: read only). (The governance/ops tools listed in your allowedPaths are writable for this lane only.)
2. Capture the exit code of every command, one per command (`"EXIT=$LASTEXITCODE"` in PowerShell, `echo EXIT=$?` in bash). Put raw values in your reports. Never type or paraphrase a commit hash: paste it from `git rev-parse` output.
3. Run commands in the FOREGROUND. Never end your turn while background jobs or child processes are running. Commit early (WIP commits allowed on your branch). Do not merge. Write only inside allowedPaths.
4. Commit and push to your own branch only: `git push origin task/lane-g1` after your final commit (never any other branch, never `main`, never force). If the push is refused, do not work around it (never set `DEUS_INTEGRATOR`); write the refusal in your REPORT and stop; the PM pushes.
5. Do not merge. Do not self-certify: never write DONE, PASS, VERIFIED or CLOSED about your own work in any file (test-runner `PASS <check>` lines are fine). Your REPORT states what you did and the raw evidence. An independent Grok review decides; the PM merges.
6. Stop and write `tasks/WG.00.12b/lane-g1/escalation.md` (then commit and push it) if you need a file outside your allowedPaths, find a bug in a read-only shared file, or find two sources that disagree in a way the brief does not settle. Never resolve an Owner question yourself.
7. Your very last output line must be `FINAL SHA: <sha>`, with the sha pasted from `git rev-parse HEAD` after a successful push (or `FINAL SHA: <sha> (push refused)`).

## Live lanes at preparation (write sets are disjoint; do not touch another lane's paths)
| Lane | Task | Role | Write set |
|---|---|---|---|
| K | WG.00.09b depth renderer Fix 1 | Claude writer | per `tasks/WG.00.09b/lane-k/lane.json` |
| Q | SIM.40.01 structure design | Claude writer | `tasks/SIM.40.01/lane-q/**` |
| W, O2, R | reviews of SIM.40.10, SIM.50.11, SIM.40.05 | Grok reviewers | their own `review_grok_*.md` files only |
| L1 | WG.65.15 ledger | Claude writer | `game/js/sim/ledger*`, `tools/sim/test_ledger*.js`, `tools/sim/fixtures/ledger/**`, `tasks/WG.65.15/**` |
| G1 | WG.00.12b (this lane) | Claude writer | the allowedPaths above |
