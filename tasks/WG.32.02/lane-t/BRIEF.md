# Lane T Brief: Blank Template Tilesets (WG.32.02, with WG.32.01 slot maps)

**NO ART GENERATION BY ANYONE. No image generation, no AI art, no drawing, painting or editing of sprite/tile pixels, no prompting an image model, no requesting art from anyone. This lane builds only a catalogue, blank templates or placement/validation tooling.**

NO ART (DEC-007, Owner). No AI generates art of any kind. That means no drawing, painting, generating, prompting an image model, upscaling, redrawing, recolouring or editing of art pixels, and no requesting art from anyone. Allowed by the DEC-007 amendment (a608ba1b): the art CATALOGUE (a manifest of every tile and sprite with sheet, slot, pixel coordinates, size and palette); BLANK template tilesets (empty grids and slot IDs generated from the catalogue); and PLACEMENT/validation tooling that places Owner-approved art into catalogue slots. A blank template contains nothing but grid lines, slot IDs/labels and one flat background. Tests use only synthetic solid-colour blocks generated by the test at run time. Test-harness screenshots are evidence, not art.

**Lane:** lane-t | **Task ID:** WG.32.02 | **Branch:** task/lane-t | **Worktree:** `C:\Users\snewt\.deus_worktrees\lane-t` | **Writer:** claude | **Reviewer:** grok (independent, later) | **Base:** main `b612bc7217349bce695e15395bd041f63673b89b` | **Source directive:** PM draft 0019-S §2, updated by PM 0028-AC §2.3 and Owner rulings of 2026-09-26

**allowedPaths** (exact; everything else is read-only):
- `tools/art/make_blank_templates.js`
- `tools/art/test_blank_templates.js`
- `tools/art/fixtures/templates/**`
- `art/templates/**` (RESERVED for the final run after Lane S merges; see Dependency. In this run commit at most `art/templates/README.md`.)
- `tasks/WG.32.02/lane-t/**`

**FORBIDDEN** (read only): every Lane S path (`art/catalogue/**` may be read once it exists on main; it does not exist yet), `game/**`, `art/reference/**`, `art/masters/**`, `art/APPROVALS.md`, `reference/**`, every other `tools/**` path. Use `tools/png_util.js` (writePNG) and `tools/png_read.js` read-only. No npm dependencies.

## Goal
A generator that turns any catalogue obeying the `deus-art-catalogue/1.1.0` contract into blank template sheets (one PNG per sheet plus a JSON sidecar) containing ONLY a flat background, 1-px grid lines, 1-px slot outlines and slot labels, so the Owner paints into exact, chart-sized slots.

## Dependency (parallel with Lane S, not blocked by it)
Lane S is writing the real catalogue now. Build and test against a private fixture that obeys the contract below: `tools/art/fixtures/templates/catalogue.fixture.json` plus `tools/art/fixtures/templates/geometry.fixture.json`. The fixture must have at least: one 4096x4096 ATLAS, one A2 768x576 RMMZ_TILESET, one `$` RMMZ_CHARACTER sheet; a 2x2-footprint slot; a LARGE_TALL 48x96 frame and a LARGE_LONG 96x48 frame (each on a 2x2 footprint stored separately); a TINY creature (24x24 footprint in a 48x48 frame); a TALL_MEDIUM parameter that is off by default (and a test that turning it on adds the 48x64 slots); RMMZ 3x4-frame character blocks (144x192, 144x384, 288x192); a paper-doll group; a ramp run of 5 cells whose heights come from `stratumPx`; an edge/cliff strip for each stratum difference 1..5; a CEILING-anchored hanging-prop slot; and a derived variant row with `slot: null`. The final run against `art/catalogue/catalogue.json` (merge main into task/lane-t, regenerate, commit `art/templates/**`) happens in a later PM-launched run after Lane S merges. Do not wait for it in this run.

## Governing geometry and scale (Owner rulings; corrects every 9-layer assumption in draft 0019-S)
- World: 32 layers, default z range -16..+15 with surface at 0 (DEC-013 as amended, addendum §13). Nothing in the catalogue, templates or tools may assume 9 layers or hard-code a layer count; read it from `art/catalogue/geometry.json`.
- Sim geometry: one square = 5 ft; one layer = 10 ft; 5 strata of 2 ft per layer (addendum §12).
- Pixel geometry (Owner, 01:55 CT 2026-09-26, relayed by PM): tile = 48 px = one 5-ft square. Layer face height = 96 px (2 tiles). Stratum pixel split default = [19, 19, 19, 19, 20] px (sums to 96). The stratum split is a PARAMETER in `geometry.json` (`stratumPx`), never a literal in code: every tool reads it, and a test must show that changing it (for example to [20,19,19,19,19] or [24,24,24,24,0]-style invalid input) changes slot heights or is rejected.
- Humans stay deliberately small: about 42 px tall on the 48-px grid (the scale chart's HUMAN 42), so imposing monsters fit a 2-tile frame. Human-scale props (doors, beds, tables, chairs, fences, chests) are sized to the 42-px human per the scale chart, NOT to true feet. The scale chart (strip `art/reference/DEUS_HUMAN_SCALE_STRIP_V1.png` + its numeric source `game/data/DEUS_ScaleRegistry.json`) is the size authority for sprites and props (DEC-016 in docs/OWNER_DECISIONS.md). Sim distances govern the simulation only.
- Character-size authority (PM 01:59 CT, derived from SRD 5.1 text in `game/data/srd51/character_options.json` race "Size" traits and the `game/data/srd51/rules.json` Size Categories table): creatures are drawn at 7 px per foot (a 6-ft human = 42 px) on the 48-px, 5-ft grid. The sim keeps the true SRD heights; these are drawn heights only.
  - Race drawn heights (all in 48x48 frames): Dragonborn 46-48 px; Half-Orc, Human and Tiefling 35-46; Elf 33-44; Half-Elf 35-42; Dwarf 28-35; Gnome 21-28; Halfling about 21. Undecided (Owner): a readability floor of 26-28 px for Gnome and Halfling with slightly larger heads is a parameter with default OFF (see below), never applied silently.
  - Sprite frame vs footprint (Owner correction, 02:04 CT): the SRD space (e.g. Large = 2x2 squares) is the combat/footprint area in the sim and is stored SEPARATELY from the sprite frame. Every creature/character entry carries both `footprint` (squares, from SRD size) and `frame` (pixels, from the frame class below). Never derive one from the other.
  - Frame classes: `TINY` = 24x24 footprint drawn in a 48x48 frame; `SMALL` and `MEDIUM` = 48x48 frame (1 tile); `LARGE_TALL` (tall-bodied Large: humanoids such as ogres and trolls) = 48 wide x 96 tall frame on a 2x2 footprint (Owner); `LARGE_LONG` (long-bodied Large: horses, big beasts) = 96 wide x 48 tall frame on a 2x2 footprint (PM proposal consistent with the Owner's call; `status: PROPOSED`). Large frames are NOT 96x96. `HUGE` (3x3 footprint) and `GARGANTUAN` (4x4+ footprint) follow the same tall/long principle but their frame sizes are TBD by the Owner: store them as parameters (`frame: null`, `status: OWNER_OPEN`, `future: true`) and generate no paint slots for them until set.
  - Undecided parameters, default OFF: `TALL_MEDIUM` (a 48x64 frame for Medium creatures up to 8 ft) and the Gnome/Halfling readability floor (26-28 px with slightly larger heads). Both exist in geometry/size data as parameters with `enabled: false`; nothing uses them unless the Owner turns them on, and a test shows that turning one on changes the affected slots.
  - RMMZ character sheet blocks are 3x4 frames: 48x48 -> 144x192; 48x96 (LARGE_TALL) -> 144x384; 96x48 (LARGE_LONG) -> 288x192. Larger blocks (e.g. 288x384, 432x576, 576x768) only once the Owner sets Huge/Gargantuan frames.
- Biome bands (PM defaults, OPEN for the Owner): LOWER2 (deep) -16..-9, LOWER1 (shallow underground) -8..-1, SURFACE 0..+3, UPPER1 +4..+9, UPPER2 +10..+15. 25 biomes = 5 bands x 5 biomes. The biome-to-band assignment and the 25 biome names are OWNER_OPEN: never invent names. `game/data/DEUS_BiomeRegistry.json` lists 5 canonical biomes (TEMP, WET, ARID, HIGH, VOLC) and forbids snow/ice; record the 5-vs-25 gap in conflicts, and use neutral placeholder ids `<BAND>_B1..B5` (flag `ownerOpen: true`) where a per-biome slot is needed before the Owner rules.

## Catalogue contract `deus-art-catalogue/1.1.0` (frozen for lanes S, T and U)
Lane S owns the schema; lanes T and U build against this contract using private fixtures. Lane S may ADD optional fields but must not rename, remove or change the meaning of any field below. If S needs to, it writes escalation.md instead.
- `art/catalogue/geometry.json`: `{schemaVersion, squareFt: 5, layerFt: 10, strataPerLayer: 5, stratumFt: 2, layerCount: 32, zMin: -16, zMax: 15, tilePx: 48, layerPx: 96, stratumPx: [19,19,19,19,20], humanPx: 42, frameClasses: {TINY:{footprintSq:[1,1], drawnFootprintPx:[24,24], frame:[48,48]}, SMALL:{footprintSq:[1,1], frame:[48,48]}, MEDIUM:{footprintSq:[1,1], frame:[48,48]}, LARGE_TALL:{footprintSq:[2,2], frame:[48,96]}, LARGE_LONG:{footprintSq:[2,2], frame:[96,48], status:"PROPOSED"}, HUGE:{footprintSq:[3,3], frame:null, status:"OWNER_OPEN", future:true}, GARGANTUAN:{footprintSq:[4,4], frame:null, status:"OWNER_OPEN", future:true}}, optionalParams: {TALL_MEDIUM:{frame:[48,64], enabled:false}, smallRaceReadabilityFloorPx:{value:[26,28], races:["GNOME","HALFLING"], enabled:false}}, pxPerFootCreature: 7, rmmzCharacterBlocks: {"48x48":[144,192], "48x96":[144,384], "96x48":[288,192]}, bands: [{id, zMin, zMax, ownerOpen: true}], decisions: [{field, source, status}]}`.
- `catalogue.json` top level: `schemaVersion`, `tileSizePx: 48`, `geometry {path, sha256}`, `palette {path, sha256}` (art/palette/deus_master_world_palette_v1.hex), `scaleChart {path, sha256}`, `sources[] {path, sha256, role}`, `sheets[]`, `entries[]`, `outOfScope[] {sourceId, reason}`.
- `sheets[]`: `{sheetId, kind: ATLAS|RMMZ_TILESET|RMMZ_CHARACTER, group {band, biome, type}, w, h, gridPx, runtimeFile|null}`. ATLAS sides are multiples of 48 and at most 4096. RMMZ sheets use the fixed sizes in docs/RMMZ_ASSET_SPEC.md (A1/A2 768x576, A3 768x384, A4 768x720, A5 384x768, B-E 768x768; `$` character sheets 3 x frameW by 4 x frameH).
- `entries[]`: `id` (`BAND_BIOME_CATEGORY_TYPE_VARIANT_STATE`), `category`, `band`, `biome`, `zMin`, `zMax` (inside geometry range), `sizeClass` (SRD size) and `frameClass` (for characters/creatures; footprint and frame stored separately), `sourceIds {catalog[], assetIndex[], brief[], ar[], manifest[], matrix[], addendum[]}`, `scaleRow` (required; a scale_chart.json row id, or a geometry row id such as `GEOM_TILE`, `GEOM_STRATUM_1..5`, `GEOM_LAYER_FACE`, `GEOM_FRAME_LARGE_TALL`, or a `size_classes.json` race row such as `RACE_HUMAN`), `envelope {wMin,wTarget,wMax,hMin,hTarget,hMax}`, `footprint {w,h}` (tiles), `anchor {type: GROUND|CEILING|WALL|CENTER, x, y}`, `paletteRampIds[]`, `frames {cols, rows, facings, rate}`, `slot {sheetId, slotId, x, y, w, h} | null` (slotId = `<sheetId>:<4-digit index>`), `runtime {kind, file, index|tileId}`, `variants {derivedFrom|null, flipH, flipV, rot, paletteSwap|null, lightingSafe}`, `paperDoll {...}|null`, `references[]`, `standardPending|null`, `alphaMode: BINARY|OWNER_OPEN`, `status: MISSING|EXISTING_UNAPPROVED|STAND_IN|STOCK|APPROVED|OUT_OF_SCOPE`, `statusWhy`.
- Slot sizing rule: slot w = 48*ceil(wMax/48), h = 48*ceil(hMax/48) per frame; geometry-derived pieces (edge/cliff strips, wall faces, ramps) take their heights from `stratumPx`/`layerPx`, never from literals. Variant rows (`derivedFrom` set) own no paint slot (`slot: null`).

## Deliverables
1. `tools/art/make_blank_templates.js --catalogue <path> [--geometry <path>] --out <dir> [--bg transparent|magenta]`: writes one PNG per sheet plus a sidecar `<sheetId>.json {sheetId, w, h, gridPx, bg, gridColour, labelColour, geometrySha256, stratumPx, slots[{slotId, entryId, scaleRow, frameClass, footprint, anchor, x, y, w, h}]}`, and `<out>/INDEX.md`. Geometry is read from the catalogue's `geometry` reference (or `--geometry`); `stratumPx`, `layerPx`, `tilePx` and the size-class frames are never literals in the code (a test greps for this).
2. Allowed pixel content, nothing else: the flat background (alpha 0 by default, or #FF00FF with `--bg magenta`); 1-px grid lines on the 48 grid in one grid colour; 1-px slot-boundary rectangles at the catalogue rects in the same grid colour; stratum guide ticks for geometry-derived slots (edge strips, wall faces, ramps) at the `stratumPx` boundaries, in the grid colour; slot labels in one label colour, drawn with a bitmap font defined in code (no font files, no anti-aliasing): slot index plus the scaleRow short code, index only when it does not fit (full text in the sidecar). Grid and label colours must NOT be in `art/palette/deus_master_world_palette_v1.hex` (so template residue can never pass Lane U's palette check).
3. Output via writePNG with no timestamps or tEXt chunks, so reruns are byte-identical.
4. `tasks/WG.32.02/lane-t/REPORT.md`: commands with raw EXIT lines, fixture template count, sha256 of each fixture template from two runs, provoked-failure evidence, final `git rev-parse HEAD`.

## Done tests (`tools/art/test_blank_templates.js`, non-zero on failure; each check has `UF_TEST_PROVOKE=templates.<check>` shown to fail it)
(1) one template per catalogue sheet, none extra; (2) exact sheet dimensions; (3) pixel census: only {bg, grid, label} colours, alpha only 0/255; (4) every slot's four boundary edges sampled at the catalogue x, y, w, h; (5) slot interiors minus label and guide pixels are pure background; (6) sidecar matches the catalogue 1:1; (7) two runs are sha256-identical; (8) grid and label colours are not in the master palette; (9) changing `stratumPx` in the geometry fixture moves the stratum guide ticks and ramp/strip slot heights accordingly, and an invalid split (not summing to `layerPx`) is refused; (10) mutants: wrong sheet size, shifted slot, missing sheet, extra colour, hard-coded stratum height; each fails. Tests write only to os.tmpdir().
Gate command: `node tools/art/test_blank_templates.js` exits 0.

## Acceptance criteria (the Grok reviewer will check these)
- Gate exits 0 in a fresh clone; every provocation and mutant fails.
- Templates contain no drawn content beyond background, grid, outlines, guide ticks and labels.
- No file outside allowedPaths changed; no PNG committed in this run.
- Stratum height is a parameter end to end.

Commit messages start `[claude] WG.32.02`.

## Standing rules (verbatim, every lane)
1. One primary writer per file set. Your write set is exactly the allowedPaths above. It is disjoint from every other live lane: K (`game/js/plugins/DEUS_Depth.js`, `game/js/plugins.js` DEUS_Depth block, `DEUS_Minimap.js`, `DEUS_Fog.js`, `DEUS_DayNight.js`, `docs/systems/DEUS_Depth.md`, `docs/systems/DEUS_Minimap.md`, `tools/test_layer_render_flat.js`, `tools/bench_render_layers.js`, `tools/test_minimap.js`, `tasks/WG.00.09b/lane-k/**`), N (`game/js/plugins/DEUS_Levels.js`, `game/js/plugins/DEUS_World.js`, `tools/test_layer_switch_inplace.js`, `tasks/SIM.00.00/lane-n/**`), M (`docs/adr/ADR-003*`, `docs/adr/README.md`, `tasks/SIM.00.01/**`), E (paused), the merged governance/ops tools (`tools/governance/**`, `tools/ops/**`: read only), the coordinator's files (`docs/STATUS.md`, `docs/VISION.md`, `docs/OWNER_DECISIONS.md`, every `*WBS*.md`, `docs/telemetry/**`: read only) and the other new lanes listed in the table at the end of this brief.
2. Capture the exit code of every command, one per command (`"EXIT=$LASTEXITCODE"` in PowerShell, `echo EXIT=$?` in bash). Put raw values in your reports. Never type or paraphrase a commit hash: paste it from `git rev-parse` output.
3. Run tests in the FOREGROUND. Never end your turn while background jobs or child processes are running. Commit early (WIP commits allowed on your branch). Do not merge. Write only inside allowedPaths.
4. Commit and push to your own branch only: `git push origin <your branch>` after your final commit (never any other branch, never `main`, never force). If the push is refused, do not work around it (never set `DEUS_INTEGRATOR`); write the refusal in your REPORT and stop; the PM pushes.
5. Do not merge. Do not self-certify: never write DONE, PASS, VERIFIED or CLOSED about your own work in any file. Your REPORT states what you did and the raw evidence. An independent Grok review decides; the PM merges.
6. Stop and write `tasks/<task>/<lane>/escalation.md` (then commit it) if you need a file outside your allowedPaths, find a bug in a read-only shared file, or find two sources that disagree in a way the brief does not settle. Never resolve an Owner question yourself.

## New lanes opened together (write sets are disjoint; do not touch another lane's paths)
| Lane | Task | Role | Write set |
|---|---|---|---|
| S | WG.20.02 art catalogue | Claude writer | `art/catalogue/**`, `docs/art/catalogue/**`, `tools/art/build_catalogue.js`, `tools/art/test_catalogue.js`, `tools/art/fixtures/catalogue/**`, `tasks/WG.20.02/lane-s/**` |
| T | WG.32.02 blank templates | Claude writer | `tools/art/make_blank_templates.js`, `tools/art/test_blank_templates.js`, `tools/art/fixtures/templates/**`, `art/templates/**`, `tasks/WG.32.02/lane-t/**` |
| U | WG.41.01 placement + validator | Claude writer | `tools/art/place_art.js`, `tools/art/validate_art.js`, `tools/art/test_place_art.js`, `tools/art/fixtures/place/**`, `docs/art/APPROVALS_FORMAT.md`, `tasks/WG.41.01/lane-u/**` |
| gap-audit | SIM.50.01 living-world gap audit | Claude writer (coordinator-launched) | `docs/audits/LIVING_WORLD_GAP_AUDIT.md`, `tasks/SIM.50.01/gap-audit/**` |
| P | SIM.60.01 SRD spell-effect audit | Claude writer | `docs/audits/SRD_SPELL_EFFECT_AUDIT.md`, `docs/audits/srd_spell_effect_audit.json`, `tasks/SIM.60.01/lane-p/**` |
| M | SIM.00.01 ADR-003 Rev 3 | Claude writer | `docs/adr/ADR-003_sim_render_split_and_lod.md`, `docs/adr/README.md`, `tasks/SIM.00.01/lane-m/**` (not review files) |
| N review | SIM.00.00 in-place switch | Grok reviewer | `tasks/SIM.00.00/lane-n/review_grok_2f2a1ff2.md` only |
