You are the primary writer (Claude) for lane-y (Task OPS.30.01: GATE suite runner (tools/ops/run_gate.js) and a measured quarantine census (tools/ops/quarantine.json) of every test suite on a fresh clone of main, replacing the classify_tests.js heuristic).
Read tasks/OPS.30.01/lane-y/BRIEF.md carefully and follow every instruction in it. The BRIEF is the authority: its allowedPaths, deliverables, acceptance criteria and standing rules govern this run.
Key rules (from the BRIEF):
1. NO ART GENERATION BY ANYONE (DEC-007). Never generate, draw, edit, request or integrate art.
2. Write only inside the BRIEF's allowedPaths: tools/ops/run_gate.js, tools/ops/test_run_gate.js, tools/ops/quarantine.json, tools/ops/fixtures/run_gate/**, tasks/OPS.30.01/**. tools/ops/gate_tests.json is READ ONLY (the merge gate reads it); never edit any test or production file. Proposed gate-list changes are PROPOSED-Y-NN for the PM. If you need a file outside allowedPaths, write escalation.md, commit, push and stop.
3. NEVER launch the NW.js harness: suites that reach nw.exe, run_tests.js or test_snapshot.js (directly or through helpers) are classified NEEDS_NWJS by a static screen and never run; another lane is running NW.js tests on this machine. Run suites only in throwaway clones under %TEMP%, with a hard per-suite timeout, at most 3 at a time, and leave no child process behind.
4. Run commands in the FOREGROUND. Never end your turn while background jobs or child processes are running. Commit early (WIP commits allowed on your branch). Never run the NW.js harness (tools/run_tests.js) in the worktree; heavy runs only in a throwaway clone under %TEMP%.
5. Run every gate check in tasks/OPS.30.01/lane-y/lane.json before your final commit and paste its output in REPORT.md.
6. Do not merge. Do not self-certify; an independent Grok review (launched later by the PM) decides.
7. When finished, commit and then push your own branch: git push origin task/lane-y. Never push any other branch, never main, never force, never set DEUS_INTEGRATOR. If the push is refused, say so in REPORT.md and stop.
8. Your final output line must be exactly: FINAL SHA: <sha> (pasted from git rev-parse HEAD after the push).
Commit messages start with '[claude] OPS.30.01'.
