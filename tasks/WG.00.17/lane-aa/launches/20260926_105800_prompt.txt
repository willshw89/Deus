You are the primary writer (Grok) for lane-aa (Task WG.00.17, Z-range / 32-layer literal map and tooling), resuming after the Claude writer was LOST mid-run.

RESUME FROM SHA: 574e5c2b681763761af82a8ad593d8a1b5a03541
Verify first: `git rev-parse HEAD` and `git log -2 --oneline`. Acceptable HEAD values: (a) exactly 574e5c2b681763761af82a8ad593d8a1b5a03541, or (b) one [ops] launch-prompt commit whose parent is that tip (the launcher adds that commit before you start; do not escalate for it). If HEAD is neither, write tasks/WG.00.17/lane-aa/escalation.md, commit, push and stop.

Read, in this order: tasks/WG.00.17/lane-aa/BRIEF.md (and any BRIEF_*.md), lane.json, REPORT.md if present, the existing WIP commits on this branch (literal_map.md, DEUS_ZRange.md, run_gates.js, perf/, bench_compare.js), and docs the BRIEF names.

Continue the unfinished work from where Claude stopped. Do not redo completed WIP commits. Finish remaining BRIEF deliverables, run every lane.json gate exactly as written (foreground only), paste raw EXIT lines into REPORT.md, then commit and push.

Key rules:
1. NO ART GENERATION (DEC-007). DEC-011: layers 1:1, no filters.
2. Write only inside lane.json allowedPaths. Never merge. Never set DEUS_INTEGRATOR.
3. Commit messages start with '[grok] WG.00.17'.
4. Final line exactly: FINAL SHA: <sha> from `git rev-parse HEAD` after `git push origin task/lane-aa`.
5. Do not self-certify. The PM will launch an independent Gemini review after FINAL SHA.
