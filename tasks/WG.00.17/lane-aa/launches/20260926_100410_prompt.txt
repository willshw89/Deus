RESUME (PM relaunch #1, 2026-09-26 ~10:05 CT): your previous session stopped correctly on escalation E1 (tasks/WG.00.17/lane-aa/escalation.md, stop commit d7d107f6). That was the right call, not a failure. Do NOT start over.
PM RULING E1-A (2026-09-26 10:03 CT) is now in BRIEF.md (section "PM ruling E1-A", PM commit bfe6240d on task/lane-aa). Re-read that section FIRST. Summary: option A is approved; options B (always bind depth 2) and C (change the fixture scene) are rejected. Under E1-A, and ONLY for this: in game/js/plugins/DEUS_Depth.js you may make a minimal edit to the depth-2-plane expectation of the layers_flat switch_same_frame check (the `want` list, ~line 1902), so that depth 2 (to - 2) is wanted only under the renderer's own rule: level to - 1 exposed and openCells on to - 1 in the view's area > 0, reusing the renderer's helpers. No other check logic, suite, provocation, required count (layers_flat 12, depth 27) or fixture scene may change. The renderer (rebuild and all rendering code) must NOT change for this.
Add a regression test under tools/zrange/** (or tools/test_zrange.js) covering both the open-cell case (depth 2 bound and wanted) and the closed-cell case (depth 2 neither bound nor wanted), with a provocation shown CAUGHT in each. The -2..+2 control (DEUS_Z_RANGE=-2..2 node tools/test_layer_render_flat.js, 12/12) must still pass, as must every gate at -16..+15 and -4..+4, including the 3-run repeats and --provoke --jobs 3. In REPORT.md, quote the exact before/after lines and cite E1-A. Rule 2 below ("never change ... DEUS_Depth.js check code") still holds for everything except this one edit. If the fix needs more than this, write a new escalation and stop.
First run git log --oneline -5 and git status, re-read escalation.md (including F2/F3, which stay PROPOSED-AA-NN follow-ups, not in scope), then finish the remaining BRIEF work listed under "Not done" there (3-run repeats, provocations, whole-suite base/tip diff, bench, REPORT.md). Commit early (WIP commits are fine) so a usage limit loses nothing.
You may push ONLY task/lane-aa (git push origin task/lane-aa). Never main, never any other branch, never force, never set DEUS_INTEGRATOR.
Your final output line must be exactly: FINAL SHA: <sha> (pasted from git rev-parse HEAD after the push).
--- original prompt follows ---You are the primary writer (Claude) for lane-aa (Task WG.00.17: make the Z range one configurable setting, expand to 32 layers -16..+15 with mandatory sparse storage in memory and saves, 2-ft strata / 10-ft layers / 5-ft squares, 9-layer test mode kept).
Read tasks/WG.00.17/lane-aa/BRIEF.md carefully and follow every instruction in it. The BRIEF is the authority: its allowedPaths, Owner facts, scope phases, tests, performance budget, acceptance criteria and standing rules govern this run.
Key rules (from the BRIEF):
1. NO ART GENERATION BY ANYONE (DEC-007). Never generate, draw, edit, request or integrate art.
2. Write only inside the BRIEF's allowedPaths (the 22 listed game/js/plugins files, only for Z-range / sparse-storage / new-layer generation / feet changes; the listed docs/systems files; tools/test_zrange.js; tools/zrange/**; tasks/WG.00.17/**). Never change game/js/plugins.js, DEUS_Test.js, the ledger, any gate tool (test_layer_render_flat.js, test_minimap.js, test_layer_switch_inplace.js, test_snapshot.js, bench_render_layers.js, run_tests.js, check_deus_syntax.js) or DEUS_Depth.js check code. If you need a file outside allowedPaths, write escalation.md, commit, push and stop.
3. Owner facts are non-negotiable: 32 layers, default split -16..+15 held as data, 5-ft squares, 10-ft layers, 2-ft strata, sparse storage for memory and saves, DEC-011 1:1 with no filters, old 5-level saves load unchanged at -2..+2. Never answer an open Owner question (Q16, the lava rule, the band table); list them in REPORT.md.
4. Keep the layers_flat (12), depth (27), minimap (24) and layer_switch_inplace (7) gates passing strictly and unchanged, at -16..+15 and at -4..+4, with provocations still caught. Meet the BRIEF's performance budget against a same-session base rerun, with machine load recorded.
5. Run commands in the FOREGROUND. Never end your turn while background jobs or child processes are running. Commit early (WIP commits allowed on your branch). Heavy NW.js runs (run_tests, bench, provocations) only in throwaway clones under %TEMP%, never in the worktree; delete them before your final commit.
6. Run every gate check in tasks/WG.00.17/lane-aa/lane.json before your final commit and paste its output in REPORT.md.
7. Do not merge. Do not self-certify; an independent Grok review (launched later by the PM) decides.
8. When finished, commit and then push your own branch: git push origin task/lane-aa. Never push any other branch, never main, never force, never set DEUS_INTEGRATOR. If the push is refused, say so in REPORT.md and stop.
9. Your final output line must be exactly: FINAL SHA: <sha> (pasted from git rev-parse HEAD after the push).
Commit messages start with '[claude] WG.00.17'.
