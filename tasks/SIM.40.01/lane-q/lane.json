{
  "lane": "lane-q",
  "taskId": "SIM.40.01",
  "branch": "task/lane-q",
  "writer": "claude",
  "reviewer": "grok",
  "allowedPaths": [
    "tasks/SIM.40.01/lane-q/**"
  ],
  "gateTests": [
    {
      "cmd": "node",
      "args": [
        "-e",
        "const fs=require('fs');const d='tasks/SIM.40.01/lane-q/';const t=JSON.parse(fs.readFileSync(d+'trace.json','utf8'));const md=fs.readFileSync(d+'SIM.40.01_STRUCTURAL_SUPPORT.md','utf8');const want=Array.from({length:12},(_,i)=>'Q-'+String(i+1).padStart(2,'0'));const S=['COVERED','PARTIAL','OWNER_QUESTION'];const r=t.requirements||[];const ids=r.map(x=>x.id);const bad=r.filter(x=>!S.includes(x.status)||!x.section||!md.includes(x.section));const miss=want.filter(w=>!ids.includes(w));const H=['## Material model','## Buildings as strata','## Support model','## Localized support recheck','## Sudden collapse','## Decay-driven collapse','## Blasts across layers','## Mass ledger','## Sparse storage and cost','## Hooks and events','## Acceptance tests','## WBS impact','## Owner questions'];const mh=H.filter(h=>!md.includes(h));const b=Buffer.byteLength(md);const oq=(t.ownerQuestions||[]).length;console.log('task',t.task,'reqs',r.length,'missing',miss.join(',')||'none','bad',bad.map(x=>x.id).join(',')||'none','missingHeadings',mh.join('|')||'none','ownerQuestions',oq,'mdBytes',b);if(t.task!=='SIM.40.01'||miss.length||bad.length||mh.length||r.length!==want.length||oq<1||b<24000)process.exit(1)"
      ],
      "timeoutSec": 60
    }
  ]
}
