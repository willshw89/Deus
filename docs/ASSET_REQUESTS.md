# ASSET REQUESTS: art the engine needs, with specs

**Claude Code** (engine) adds a request here whenever a feature needs art. **Gemini** (art) makes the assets to these specs. Claude Code checks each delivery against its spec and integrates it, and **the user approves** every asset before it counts as final (VISION V11).

Every rule in `docs/ART_STANDARD.md` applies. **The look is High-Resolution 2.5D Ultima VII Oblique contained in 1-square tile (48×48 px)** (approved by user 2026-09-18, night): authentic 45° up-and-left lean (showing Top, South, and East faces), micro-dithered shading, and U7 Daylight Palette (`art/palette/uf.hex`). All assets fit strictly inside the 48×48 tile boundary. If a spec here seems wrong or impossible, write it under the request's **Notes** and tell the user. Don't silently change the asset or the spec.

## Status flow
`REQUESTED` → `IN PROGRESS` (Gemini) → `DELIVERED` (files in place, sidecar written) → `CHECKED` (Claude Code: ART_STANDARD §8 checks pass and it works in the engine) → `APPROVED` (user) → `INTEGRATED`
A delivery that fails its check goes back to `IN PROGRESS`, with the reason in Notes.

Briefs: every asset has a brief in `docs/asset_briefs/` (segments; start with `INDEX.md`). The brief is the spec; this table is the queue and status.

## Shared spec (applies to every request unless it says otherwise)
Set 2026-09-19 by the user (VISION V2, V3, V44, V9). `docs/ART_STANDARD.md` has the full rules; the prompts in `docs/handoffs/GENERATOR_PROMPTS.md` carry them to the generators.

| Item | Spec (HD pixel art in the style of Final Fantasy VI) |
|---|---|
| View | Flat 3/4 top-down RPG view (the RPG Maker default): upright sprites, walls show top and front face; no lean, no isometric, no perspective |
| Definition | 1 art pixel = 1 screen pixel on the 48 px grid, detail using every pixel |
| Grid cell | 48×48 screen pixels (1 RMMZ tile) |
| Sizes | Against the grid square, ART_STANDARD §2: a grown person fills about one square (48×48 frame); larger beings and trees use 96×96 or 96×144 frames; everything stands on the bottom-centre of its frame |
| Facings | **8 for every action** (S, SW, W, NW, N, NE, E, SE; user 2026-09-19: "Actions are also 8 way"): stand, idle, walk, work, attack, cast, hurt and death each have all eight; diagonals are true three-quarter turns; the east side may mirror the west. **No carry** (VISION V89, 2026-09-19): carrying is not drawn, the load is written in the profile |
| Character sheet | AR-600: 8 rows × 20 columns; generators deliver one image per action (no carry image), the cleaning tool assembles the sheet and fills column 7 with the stand frame |
| Palette | `art/palette/uf.hex` (256 colours); the cleaning tool snaps to it |
| Alpha | 0 or 255 only; no baked shadows (the engine draws shadows) |
| Generation | 4× canvas (one grid square = 192×192), flat magenta `#FF00FF` background, kept in `art/raw/` |
| Placeholders | Stock RPG Maker MZ art (V9); the `U7_` stand-ins stay on disk, unused |
| Original art | Passes `tools/art_check.js --native` and `tools/originality_check.js`, then the user approves it |

### Sprite sheet + sidecar format (what the engine reads)
Every object or character image comes with a JSON sidecar of the same name (`UF_Human_Male.png` + `UF_Human_Male.json`). Sheets are a grid: **one row per facing, one column per frame**, all frames the same size, in final pixels (1 art pixel = 1 screen pixel).

```json
{
  "id": "human_male",
  "frameWidth": 84,
  "frameHeight": 84,
  "anchor": [83, 83],
  "footprint": [1, 1],
  "heightLifts": 5,
  "facings": ["S", "W", "E", "N"],
  "animations": { "stand": [0], "walk": [1, 0, 2, 0] },
  "frameMs": 150,
  "standInSource": "SHAPES.VGA shape 458 frames 0-2, 16-18 (only for U7_ stand-ins)"
}
```
- `anchor` is the pixel inside a frame that sits on the **bottom-right corner of the footprint** (for U7 shapes, the decoded hotspot × 3).
- Objects without facings (trees, rocks) use `"facings": ["S"]`. States such as `"stump"` or `"harvested"` go in `animations` as single-frame entries.
- Until the engine's sidecar loader exists (Claude Code, next), a stand-in may also be delivered as a standard RMMZ `$`/`!` 3×4 sheet. Say so in Notes.

## Requests

| ID | Asset | Needed for | Priority | Status |
|---|---|---|---|---|
| AR-001 | Ground tiles: grass (2 variants), dirt, riverbed water with banks | Every area (Slice 0 world) | High | DELIVERED (stand-in `U7_Ground_A2.png`, `U7_Outside_A2.png`, 8×4 autotile layout, shapes 4, 23, 5, 3× integer) |
| AR-010 | Human, male (naked) | Adam; later all humans | High | REQUESTED (U7 stand-ins retired 2026-09-19 (V9: stock placeholders); stock in use: `$UF_Stock_People1_4` = RMMZ `People1` character 4, a villager in brown work clothes, also written over `$Adam.png`, which UF_Colonists hard-codes; the U7 file is kept as `$Adam.u7bak.png`; clothing tiers: AR-501) |
| AR-011 | Human, female (naked) | Eve | High | REQUESTED (U7 stand-ins retired 2026-09-19 (V9: stock placeholders); stock in use: `$UF_Stock_People1_5` = RMMZ `People1` character 5, a braided woman in an apron, also written over `$Eve.png`, which UF_Colonists hard-codes; the U7 file is kept as `$Eve.u7bak.png`; clothing tiers: AR-501) |
| AR-020 | Fruit tree (the glade's tree) | Glade, food source | High | REQUESTED (U7 stand-ins retired 2026-09-19 (V9: stock placeholders); stock in use: RMMZ `Outside_B` tile 176, the large tree drawn 2×2, tinted #ffd0a0 for fruit_tree and #9a9a8a for fruit_tree_bare) |
| AR-021 | Wild tree, 3 variants, standing + stump | World generator, felling | High | REQUESTED (U7 stand-ins retired 2026-09-19 (V9: stock placeholders); stock in use: `Outside_B` tile 176, the large tree drawn 2×2, for oak; tile 221 over 229, the snowy conifer drawn 1×2 and tinted #6aa86a, for pine; tile 156 for the stump; the legacy editor map Map002 still names the U7 stand-ins `!$TimberOak` (46 events), `!$PineTree` (82) and `!$FruitTree` (1), not drawn in play, see STATUS → Stand-ins) |
| AR-022 | Boulder, 2 variants | World generator, stone gathering | Medium | REQUESTED (U7 stand-ins retired 2026-09-19 (V9: stock placeholders); stock in use: `Outside_B` tile 159, grey boulder, for granite_boulder; tile 167, rust-brown boulder, for ironstone; the legacy editor map Map002 still names the U7 stand-ins `!$GraniteBoulder` (85 events) and `!$IronstoneDeposit` (64), not drawn in play, see STATUS → Stand-ins) |
| AR-023 | Bush, 2 variants | World generator | Medium | REQUESTED (U7 stand-ins retired 2026-09-19 (V9: stock placeholders); stock in use: `Outside_B` tile 166 for bush; berry bush: Gemini's own `!$UF_BerryBush.png` / `!$UF_BerryBush_Bare.png` in use, not a stock asset; the legacy editor map Map002 still names the U7 stand-in `!$BerryBush` (57 events), not drawn in play, see STATUS → Stand-ins) |
| AR-030 | Look cursor | Cursor (Slice 0, deliverable 7) | High | DELIVERED (`game/img/system/U7_Cursor.png`, 48×48 2-frame pulse gold bracket) |
| AR-031 | Unit selection marker: **a bright square outline with iron corners under the selected unit's feet, pulsing, Shining Force style** (48×48, open middle; user spec 2026-09-18) | Unit selection (`UF_Stance` draws it between the stance square and the sprite; 2–3 frames if animated) | Medium | DELIVERED (`art/masters/ui_target_square.png`, `ui_target_square.json`: 3-frame pulse sheet, 144×48, 11 colors, PASS originality check 0.502..0.543 >= 0.28, review in `art/review/batch1_ground_markers_review.png`) |
| AR-032 | Look panel frame | Look panel (deliverable 7) | Medium | DELIVERED (`art/masters/ui_tooltip.png`, `ui_tooltip.json`: 48×48 9-slice slab, 8px border, brass rule and corner nail heads, 7 colors, PASS originality check 0.301 >= 0.28, review in `art/review/batch1_ground_markers_review.png`) |
| AR-033 | UI window skin (replaces RMMZ `img/system/Window.png`) | Colonist card, look panel, all windows | Medium | DELIVERED (`art/masters/ui_window_skin.png`, `ui_window_skin.json`; authentic HD FF6 style carved dark oak with Celtic knotwork, brass rosette bosses, gold trim, 31 colors, PASS originality check (0.489 >= 0.28), review in `art/review/ui_window_skin_review.png`) |
| AR-034 | Stance squares: a flat 48×48 marker under a unit's feet in three colors (green friendly, yellow indifferent, red hostile), readable at 1× | V32, `UF_Stance` | Medium | DELIVERED (`art/masters/ui_stance_friendly.png`, `art/masters/ui_stance_indifferent.png`, `art/masters/ui_stance_hostile.png`: 48×48 flat markers, 2px beveled frame, chamfered corners, 1px dither field, corner rivets, 5 colors each, review in `art/review/batch1_ground_markers_review.png`) |
| AR-035 | Designation marker: a flat 48×48 outline with small glyphs for chop / gather / pick / quarry / mine / hunt / build / haul | V34, `UF_Interact` right-click menu | Medium | DELIVERED (`art/masters/ui_designation_markers.png`, `ui_designation_markers.json`: 15 job frames of 48×48, 27 colors, PASS originality check 0.367..0.460 >= 0.28, review in `art/review/batch2_interface_review.png`) |
| ~~AR-040~~ | ~~Cave floor tiles~~ | Withdrawn 2026-09-18: no underground layer (V20 retired) | — | WITHDRAWN (Gemini's `U7_Ground_A2.png` row 1 stays as reference) |
| ~~AR-041~~ | ~~Cave rock tiles~~ | Withdrawn 2026-09-18 | — | WITHDRAWN |
| ~~AR-042~~ | ~~Cave mouth~~ | Withdrawn 2026-09-18 | — | WITHDRAWN (`!$U7_CaveMouth.png` delivered; unused) |
| ~~AR-043~~ | ~~Way up~~ | Withdrawn 2026-09-18 | — | WITHDRAWN (`!$U7_CaveLadder.png` delivered; unused) |
| AR-044 | Ore and stone outcrops: iron, copper, gold, crystal cluster + small crystals, cave boulder (now surface: mountains, badlands, rock desert) | Biomes, mining | Medium | DELIVERED (`copper_outcrop.png`, `gold_outcrop.png`, `crystal.png`, `crystal_small.png`: 48×48 native masters with AR-600 sidecars, 4× raw canvases on magenta; all <= 14 colors from `art/palette/uf.hex`, PASS originality check min 0.434 >= 0.28, review in `art/review/minerals_batch2_lineup_4x.png`) |
| AR-050 | Generic person, 4 facings, walk frames | Arrivals (V19), test units | Medium | REQUESTED (U7 stand-ins retired 2026-09-19 (V9: stock placeholders); faction members and arrivals draw the stock cuts listed under AR-400; test units in the plugin test suites still name `$U7_Townsman`, `$U7_Ranger`, `$U7_Guard`, `$U7_Goblin` and `$People1`, which needs a code change; the RMMZ editor data still names `$U7_Miner`, `$U7_DwarfGuard`, `$U7_Blacksmith` (Actors.json actors 2, 5, 8 and Map001 events) and `$U7_Goblin` (a Map001 event), not drawn in play, see STATUS → Stand-ins) |

| AR-100 | Biome ground tiles: an A2 sheet with the 22 ground kinds in the catalog's `groundKinds` order (meadow, lush grass, dry grass, scrub soil, leaf litter, needle floor, jungle floor, tundra, snow, ice, sand, stony, red clay, bare rock, rock face, mud, swamp mud, dirt, blighted grass, flowering grass, ash, scree) | Biomes (V27/V30), `UF_Tiles` | High | PARTLY DELIVERED (`meadow.png`, `tropical_grass.png`, `dry_grass.png`, `shrub_soil.png` A2 blocks, slots 0–3; code-drawn `UF_GenGround_A2` in use for others) |
| AR-101 | Water A1 autotiles: the nine kinds in `water.surface` (fresh, pond, marsh, swamp, icy, brackish, salt, deep, blighted), every autotile piece filled, 3 animation frames | Biomes, rivers, lakes, coast | High | REQUESTED (stock RMMZ `Outside_A1` kinds 0,1,2,3,4,8,10,12,14 in use) |
| AR-102 | Trees per biome family, standing + stump: oak, birch, pine, snow fir, fruit tree (with fruit / picked), flat-top, swamp, mangrove, broadleaf giant, palm, dead, blighted | Biomes, felling (states: standing, stump) | High | PARTLY DELIVERED (`oak`, `birch`, `pine`, `fir_snow`, `fruit_tree`, `fruit_tree_bare`, `tree_savanna`, `tree_swamp` masters in 96×96 frames with 4 sway frames; `stump` in 48×48 frame and `!$UF_Stump.png` charset; all 2 squares vertical, upright, PASS originality check; remaining 5 trees requested for Batch 3) |
| AR-103 | Small plants: shrub, desert shrub, snow bush, cactus ×2, tall grass, reeds, wildflowers (4 colors), fern, lichen, wild grain, wild wheat, lily pads, berry bush (full / picked) | Biomes, gathering (states: full, gathered) | Medium | PARTLY DELIVERED (`art/masters/{berry_bush,berry_bush_bare,bush,desert_shrub,snow_bush,cactus,cactus_tall,grass_tuft,reeds,fern,wildflowers}.*`, `game/img/characters/!$UF_{BerryBush,BerryBush_Bare,Bush,DesertShrub,SnowBush,Cactus,CactusTall,GrassTuft,Reeds,Fern,Wildflowers}.png`: Batch 1 and Batch 2 delivered on Rule V81 micro/sub-square scale, 0 purple fringe, <= 32 colors, PASS originality check; review in `art/review/batch1_bushes_lineup_4x.png` and `art/review/batch2_plants_showcase_4x.png`) |
| AR-104 | Site pieces: wooden wall (palisade), stone wall, rubble, fallen pillar, old bones, straw bed, work stone, **stockpile marker** (a flat dashed square); each intact + ruined | History sites and ruins, colony building | High | PARTLY DELIVERED (`!$WallWood_Set.png` [magenta box fixed, alpha 0 transparent], `!$WallStone_Set.png`, `!$UF_Straw_Bed.png`, `!$UF_Stockpile.png`, `!$UF_Crib.png`, `!$UF_Workbench.png` [authentic FF6 timber carpentry bench, 21 colors, PASS originality]; Gemini's own `!$UF_Rubble` in use; review in `art/review/camp_assets_showcase_4x.png`) |
| AR-105 | Campfire: unlit (stone ring with wood) and lit (3-frame flame), plus a burnt-out state | Hearth, cooking | High | DELIVERED (`!$UF_Campfire.png`: unlit stone ring hearth `art/masters/campfire.png` and 3-frame animated crackling flame `art/masters/campfire_lit.png`, 31 colors, binary alpha, PASS originality check min 0.415 >= 0.28, review in `art/review/camp_assets_showcase_4x.png`) |
| AR-120 | Region looks: cursed and blessed variants of grass and forest floor | Region modifiers | Low | REQUESTED (code-drawn `cursed_grass`, `blessed_grass`, `ash` kinds in `UF_GenGround_A2`) |
| AR-200 | Ground items (25 kinds), also used as gump icons | Items on the ground, Wave 2 | High | PARTLY DELIVERED (`firewood.png`, `stone.png`, `ore_iron.png`, `ore_copper.png`, `gold.png`, `gem_rough.png`, `gem_cut.png`, `bar_iron.png`, `berries.png`, `fruit.png`, `straw.png`, `mushroom.png`, `root.png`, `seeds.png`, `fiber.png`, `meat_raw.png`, `meat_cooked.png`, `fish.png`, `hide.png`, `bone.png`, `wool.png`, `bar_copper.png`, `charcoal.png`, `feathers.png`, `leather.png`, `!$UF_Item_BarCopper.png`, `!$UF_Item_Charcoal.png`, `!$UF_Item_Feathers.png`, `!$UF_Item_Leather.png`: Batches 1–6 delivered with 48×48 ground and 32×32 icon masters, raw 4× canvases on magenta, RMMZ charsets, and valid AR-600 sidecars; review in `art/review/batch1_items_showcase.png` .. `batch6_items_showcase.png`) |
| AR-201 | Tools and clothing as ground items: stone knife, stone axe, stone pick, woven wrap, hide cloak | Crafting (V35) | High | REQUESTED (U7 stand-ins retired 2026-09-19 (V9: stock placeholders); stock `IconSet.png` icons in use: stone knife `!$UF_Icon_120` (#c8b8a0), stone axe `!$UF_Icon_99` (#c8b8a0), stone pick `!$UF_Icon_216`, woven wrap `!$UF_Icon_136`, hide cloak `!$UF_Icon_138`) |
| AR-300 | Construction: walls (3 materials, all segment pieces), floors, door, stairs, ramp, campfire, beds, lean-to | Building one segment at a time, Wave 3 | High | PARTLY DELIVERED (`!$WallWood_Set.png`, `!$WallStone_Set.png`, `!$UF_Door_Wood.png`, `!$UF_Door_Stone.png`, `!$UF_Straw_Bed.png`, `!$UF_Stockpile.png` delivered for review) |
| AR-400 | People: 7 species × male/female, one-tile 48×48 sheets on the AR-600 grid (8 facings for every action: stand, walk, work, attack, cast, hurt, death, idle; **no carry image** since VISION V89, 2026-09-19: carrying is not drawn, and a `_carry` image already delivered is not used), plus the age stages of AR-601 | Factions, colonists, arrivals, test units | High | PARTLY DELIVERED (`human_male_adult_idle.png`, `human_male_adult_walk.png`, `human_male_adult_work.png`, `human_male_adult_carry.png`, `human_male_adult_attack.png`, `human_male_adult_cast.png`, `human_male_adult_hurt.png`, `human_male_adult_death.png`: adult human male complete 8-action set delivered on AR-600 standard, `$UF_Human_Male.png`; `human_female_stand.png`, `game/img/characters/$UF_Human_Female.png` female adult settler delivered with 4 facings S, W, E, N matching male scale/anchor, PASS originality 0.493..0.522 >= 0.28; review in `art/review/human_settlers_pair_showcase_4x.png`; stock cuts in use for others: human `$UF_Stock_People4_6`, `$UF_Stock_People4_5`, `$UF_Stock_People3_5`, `$UF_Stock_People2_1`; elf `$UF_Stock_Actor2_6`, `$UF_Stock_People4_7`, `$UF_Stock_Actor2_7`, `$UF_Stock_Nature_6`; dwarf `$UF_Stock_People2_6`, `$UF_Stock_People2_7`, `$UF_Stock_People4_4`, `$UF_Stock_Actor1_3`; goblin `$UF_Stock_People1_0`, `$UF_Stock_People1_1`, `$UF_Stock_People2_2`, `$UF_Stock_People2_3`; orc `$UF_Stock_Actor2_4`, `$UF_Stock_Actor2_3`, `$UF_Stock_Evil_1`, `$UF_Stock_Actor2_5`; gnome `$UF_Stock_People1_6`, `$UF_Stock_Actor1_5`, `$UF_Stock_People2_0`, `$UF_Stock_Actor3_3`; automaton `$UF_Stock_SF_People3_4`, `$UF_Stock_SF_People3_6`, `$UF_Stock_SF_People3_7`; start pair: AR-010, AR-011, AR-501) |
| AR-401 | Wildlife: the 24 species in `wildlife.species` (deer, boar, aurochs, wild horse, wild sheep, hare, fowl, rat, wolf, jackal, fox, arctic fox, wildcat, serpent, hawk, songbird, bat, giant spider, troll, bog horror, sand stalker, restless dead, ice wraith): 4 facings × stand/walk, plus a dead/carcass frame | Starting population, hunting | High | PARTLY DELIVERED (`boar_*.png`, `hare_*.png`, `wolf_*.png`, `game/img/characters/$UF_Boar.png`, `$UF_Boar_8D.png`, `!$UF_Boar_Carcass.png`, `game/img/characters/$UF_Hare.png`, `$UF_Hare_8D.png`, `!$UF_Hare_Carcass.png`, `game/img/characters/$UF_Wolf.png`, `$UF_Wolf_8D.png`, `!$UF_Wolf_Carcass.png`: boar, hare, and wolf complete 8-way 7-action suites [idle, walk, action, attack, graze, hurt, death] delivered and tested live in-engine; `deer_idle.png`, `deer_walk.png`, `deer_work.png`, `deer_hurt.png`, `deer_death.png`, `game/img/characters/$UF_Deer.png` + sidecars: complete 8-way deer stag suite; `giant_spider_idle.png`, `art/raw/giant_spider_idle.png`, `art/masters/giant_spider_idle.png`, `game/img/characters/$UF_GiantSpider.png`: monster group delivery 1 idle delivered alone for art director review; U7 stand-ins retired 2026-09-19 (V9: stock placeholders); stock cuts in use, made by tools/extract_stock_characters.js: deer `$UF_Stock_Nature_3`; boar `$UF_Stock_Nature_2` (#7a5a40); aurochs `$UF_Stock_Vehicle_4` (#6a5040); wild horse `$UF_Stock_Vehicle_4` (#b08858); wild sheep `$UF_Stock_Nature_2`; hare `$UF_Stock_Nature_2` (#c8a888); fowl `$UF_Stock_Monster_0` (#f0dcb8); rat `$UF_Stock_Nature_2` (#8888a0); wolf `$UF_Stock_Nature_0`; jackal `$UF_Stock_Nature_3` (#b8a890); fox `$UF_Stock_Nature_3` (#ff9a6a); arctic fox `$UF_Stock_Monster_4` (#e8f0ff); wildcat `$UF_Stock_Nature_1`; serpent `$UF_Stock_Vehicle_2` (#70e060); hawk `$UF_Stock_Vehicle_2` (#c09070); songbird `$UF_Stock_Nature_5`; bat `$UF_Stock_Vehicle_2` (#585068); giant spider `$UF_Stock_SF_Monster_2`; monsters: AR-402. Stock has no deer, sheep, hare, rat, fowl, bird, snake, spider or ox, so these are look-alikes told apart by tint) |
| AR-402 | Monsters: troll, bog horror, sand stalker, restless dead, ice wraith | Starting population | Medium | REQUESTED (U7 stand-ins retired 2026-09-19 (V9: stock placeholders); stock cuts in use: troll `$UF_Stock_Monster_1` (#c0c8b0), bog horror `$UF_Stock_BigMonster1_r1` (one row of `$BigMonster1`, 96×96 frames, always facing the viewer), sand stalker `$UF_Stock_SF_Monster_6` (#e8d090), restless dead `$UF_Stock_Monster_6`, ice wraith `$UF_Stock_Monster_5` (#a0d8ff)) |
| AR-403 | Domestic animals: 4 | Taming | Low | REQUESTED (U7 stand-ins retired 2026-09-19 (V9: stock placeholders); the catalog has no domestic species yet; their wild forms draw stock cuts: horse and aurochs `$UF_Stock_Vehicle_4`, sheep `$UF_Stock_Nature_2`, fowl `$UF_Stock_Monster_0`, wildcat `$UF_Stock_Nature_1`, wolf `$UF_Stock_Nature_0`) |
| AR-500 | Workshops (10), furniture (6), tool and weapon icons (8) | Crafting, Wave 5 | Medium | REQUESTED |
| AR-510 | Combat-chain workshops, one cell each (V44, V55): furnace, smithy, bowyer's bench, fletcher's bench, tanning rack, weapon rack; built state (cold/lit pair welcome for furnace and smithy); spec `docs/handoffs/HANDOFF_combat_chains.md` | `docs/design/COMBAT_CHAINS.md` §5; the society plan builds them | High | REQUESTED (stock RMMZ tiles in use: `Outside_B` 148 furnace (tinted), 149 tanning rack; `Inside_C` 404 smithy, 403 bowyer's and fletcher's benches (tinted), 400 weapon rack) |
| AR-511 | Weapons, armor, ammunition and chain goods as ground items (23): charcoal, copper bar, feathers, leather, arrows, short bow, long bow, sling, club, spear, iron dagger, short sword, long sword, iron axe, copper mace, leather cap, iron helmet, leather armor, iron mail, leather leggings, iron greaves, wooden shield, iron shield; one frame each, also the inventory icon later | `docs/design/COMBAT_CHAINS.md` §2; V49 inventory grid | High | REQUESTED (U7 stand-ins retired 2026-09-19 (V9: stock placeholders); stock `IconSet.png` icons in use as `!$UF_Icon_<n>` sheets: charcoal 167, copper bar 313 (#e0a070), feathers 297, leather 291 (#d09060), arrows 225 (a quill: the icon set has no arrow), short bow 102, long bow 102 (#c8a070), sling 114, club 110, spear 107, iron dagger 120, short sword 96, long sword 97, iron axe 99, copper mace 98, leather cap 150, iron helmet 132, leather armor 153, iron mail 135, leather leggings 140, iron greaves 141, wooden shield 129, iron shield 128) |
| AR-512 | Held-weapon layers (spear, short bow, long bow, sling, club, dagger, short sword, long sword, iron axe, mace) and shield layers (wood, iron) on the AR-600 grid | V41/V45 attack frames with the weapon in hand; the layer compositor (AR-600 engine side) | Medium | PARTLY DELIVERED (Batch 1: stone_axe, stone_knife, stone_pick, shield_wood masters and RMMZ charsets: `$UF_Layer_stone_axe.png`, `$UF_Layer_stone_knife.png`, `$UF_Layer_stone_pick.png`, `$UF_Layer_shield_wood.png`) |
| AR-501 | Clothing tiers for humans: 1 woven wraps, 2 hides, 3 tailored, male and female, 4 facings × walk | V35 (colonists make clothes) | High | PARTLY DELIVERED (Tier 1 woven grass wrap: fiber_wrap master and RMMZ charset: `$UF_Layer_fiber_wrap.png`) |
| AR-600 | **Character sheet standard v2** (layered, animated; 8 facings per VISION V3 revised 2026-09-19: rows S, SW, W, NW, N, NE, E, SE, the east side may mirror the west; 20 columns: 0 stand, 1-3 walk, 4-6 work, 7 the stand frame again (formerly carry: not drawn since V89), 8-10 attack, 11-13 cast, 14 hurt, 15-17 death, 18-19 idle): one frame grid for every person and creature; layers = body (species × gender × age stage), clothing tier, held tool or weapon, shield, effect; animations = stand, walk, work (mine, chop, gather), attack, cast, hurt, death, idle, each in all 8 facings (V3, user 2026-09-19: "Actions are also 8 way"); generators deliver one image per action (ART_STANDARD §4), with no carry image | V41; the engine's layered character renderer (next build) | High | REQUESTED (spec below; today's sheets are single-layer 3×4 walk sheets) |
| AR-601 | Age stages: baby, child, teen sprites for each playable species (body layer only, on the AR-600 grid) | V40 life cycle (next build) | High | REQUESTED (adults only today) |
| AR-800 | Core UI images: window skin (with AR-033), `Balloon.png` (speech and thought balloons: bark bubble, sleep, work), `ButtonSet.png` (touch buttons), `IconSet.png` (32×32 icons: every item type in `items.types`, labors, combat modes), and the four container gumps (chest, barrel, backpack, sack) for the character sheet | UF_Visuals barks, UF_Items icons, the character sheet (V49), the faction menu | Medium | PARTLY DELIVERED (`art/masters/ui_badges.png`, `ui_dialogue.png`, `ui_context_menu.png`, `ui_colonist_card.png`, `ui_ledger_and_chronicle.png`, `ui_window_skin_cursor_buttons_buttonset.png`: badges, conversation window, right-click menu, card parts, ledger parts, touch buttons; all <= 32 colors, PASS originality check, review in `art/review/batch2_interface_review.png`) |
| AR-700 | **Face sets in the U7 portrait style** (a bust with a plain dark background, 96×96 exported), per species × gender × age stage, 4–8 variants each, in RPG Maker's face-sheet layout (4 columns × 2 rows of 144×144, faces centred) | V49 character sheet (portrait) | High | PARTLY DELIVERED (`game/img/faces/UF_Settler_Male.png`, `art/masters/face_human_male_adult.png`, `game/img/faces/UF_Faces_Human_Male_Adult.png`, `art/masters/face_human_female_adult.png`, `game/img/faces/UF_Faces_Human_Female_Adult.png`, `art/masters/face_human_male_elder.png`, `game/img/faces/UF_Faces_Human_Male_Elder.png`, `art/masters/face_human_female_elder.png`, `game/img/faces/UF_Faces_Human_Female_Elder.png`: Human complete: male/female adult & elder, 4 variants × 3 moods [neutral, content, angry], 32 colors, PASS originality check) |
| AR-701 | Character-sheet UI: inventory grid cell frame, equipment slot frames (helmet, weapon, shield, torso, legs), page tabs, and the faction behaviors menu's icons (labors, combat modes), in the window skin's style | V49, V43 | Medium | DELIVERED (`art/masters/ui_character_sheet.png`, `ui_character_sheet.json`: 288×256 parts sheet: inventory cell, 5 equipment slots, front/back tabs, 152×152 face bezel; 13 colors, PASS originality check 0.440 >= 0.28, review in `art/review/batch2_interface_review.png`) |

**Combat gear and workshops (VISION V55, added 2026-09-18 night by Claude Code; spec below and in `docs/asset_briefs/SEG-18_combat_gear_and_workshops.md`, delivery order in `docs/handoffs/HANDOFF_combat_gear.md`):**

| ID | Asset | Needed for | Priority | Status |
|---|---|---|---|---|
| AR-900 | **Weapon layers (V55):** club, spear, iron dagger, short sword, long sword, iron axe, mace, short bow, long bow, sling as `held` layers on the AR-600 grid (the AR-600 grid: 20 columns × 8 rows of 48×48 frames, every action in all 8 facings; painted wherever the weapon is in the hand), each with its 48×48 ground and inventory icon. Spec: one brief per weapon (ids `club`, `spear`, `dagger_iron`, `sword_short`, `sword_long`, `axe_iron`, `mace`, `bow_short`, `bow_long`, `sling`) in `docs/asset_briefs/SEG-18_combat_gear_and_workshops.md` | UF_Combat's attack frames on the map (V45), the `weapon` slot of the character sheet (V49), `$UF_held_<itemId>` (AR-600), `items.types[].image` | High | REQUESTED (nothing is drawn in the hand today; no stand-in exists for any held layer; the ground and inventory icons are stock `IconSet.png` icons since 2026-09-19 (V9): `!$UF_Icon_110` club, `!$UF_Icon_107` spear, `!$UF_Icon_120` dagger, `!$UF_Icon_96` short sword, `!$UF_Icon_97` long sword, `!$UF_Icon_99` iron axe, `!$UF_Icon_98` mace, `!$UF_Icon_102` both bows, `!$UF_Icon_114` sling) |
| AR-901 | **Shield layers (V55):** wooden shield, iron shield as `shield` layers on the AR-600 grid (painted in every column but sleep; composed under the held layer) with their ground icons. Spec: `shield_wood`, `shield_iron` in SEG-18 | the `shield` slot (V49), `$UF_shield_<itemId>` (AR-600), armor class +2 (V47) | High | PARTLY DELIVERED (Wooden shield: `shield_wood` master and RMMZ charset: `$UF_Layer_shield_wood.png`) |
| AR-902 | **Head layers (V55):** leather cap, iron helm as `head` layers on the AR-600 grid (every column but sleep) with their ground icons. Spec: `helmet_leather`, `helmet_iron` in SEG-18 | the `head` slot (V49), `$UF_head_<itemId>`, the body-part injury roll (V47) | Medium | REQUESTED (no layer stand-in; ground icons are stock `IconSet.png` icons since 2026-09-19 (V9): `!$UF_Icon_150` leather cap, `!$UF_Icon_132` iron helm) |
| AR-903 | **Torso and legs layers (V55):** leather armor and iron mail (`torso`), leather leggings and iron greaves (`legs`) on the AR-600 grid (every column but sleep) with their ground icons; the clothing tiers (AR-501, the AR-600 clothes layers) stay under them. Spec: `armor_leather`, `mail_iron`, `leggings_leather`, `greaves_iron` in SEG-18 | the `torso` and `legs` slots (V49), `$UF_torso_<itemId>`, `$UF_legs_<itemId>`, armor class (V47) | Medium | REQUESTED (no layer stand-in; ground icons are stock `IconSet.png` icons since 2026-09-19 (V9): `!$UF_Icon_153` leather armor, `!$UF_Icon_135` iron mail, `!$UF_Icon_140` leather leggings, `!$UF_Icon_141` iron greaves) |
| AR-904 | **Ammunition and chain materials (V55):** arrows (a bundle of twelve as the ground icon, plus the quiver as a `back` layer on the AR-600 grid), charcoal, iron bar (replaces the `!$U7_Item_MetalBar` stand-in), copper bar, leather, feathers: 48×48 ground items that double as the inventory icons of the character sheet. Spec: `arrows`, `charcoal`, `bar_iron`, `bar_copper`, `leather`, `feathers` in SEG-18 | UF_Items (`items.types[].image`), the character-sheet grid (V49), ranged ammunition (V55), `$UF_back_arrows` | High | PARTLY DELIVERED (`bar_iron.png`, `bar_copper.png`, `charcoal.png`, `leather.png`, `feathers.png`, `!$UF_Item_BarCopper.png`, `!$UF_Item_Charcoal.png`, `!$UF_Item_Leather.png`, `!$UF_Item_Feathers.png`: all materials delivered with 48×48 ground and 32×32 icon masters, RMMZ charsets, and valid AR-600 sidecars; review in `art/review/batch6_items_showcase.png`; arrows and quiver pending) |
| AR-905 | **Workshop objects (V55):** furnace (unlit, and lit with 3 flame frames), smithy with anvil, bowyer's bench, fletcher's bench, tanning rack with a hide, weapon rack (empty, and full): one 48×48 square each, 2.5D, blocking. Spec: `furnace`, `smithy`, `bowyer_bench`, `fletcher_bench`, `tanning_rack`, `weapon_rack` in SEG-18 | UF_Objects (`objects[].image`), the craft job's `at` workplace (UF_Jobs), the society plan's arming steps (WORLD_ARCHITECTURE §2.10) | High | REQUESTED (no image until the catalog entries land; they stand on the map first, so they are delivered first) |
| AR-906 | **Combat effects (V55):** hit flash (3 frames over the struck sprite), arrow in flight (4 flight directions plus a sling stone), blood spatter decal (3 variants, flat, drawn under units). Spec: `ui_fx_hit`, `ui_fx_arrow`, `ui_fx_blood` in SEG-18 | UF_Combat (V45, V47): `$UF_fx_hit`, `$UF_fx_arrow`, `!$UF_fx_blood`, read by fixed name | Medium | PARTLY DELIVERED (`$UF_fx_hit.png`, `!$UF_fx_blood.png`: hit flash 3 frames and blood spatter 3 variants delivered; arrow and work effects in progress) |

**Vertical world (VISION V80, `docs/design/VERTICAL_WORLD.md`; added 2026-09-19 by Claude Code; spec in "AR-1200 to AR-1219 Vertical world" below; slots, look, prompt and order in `docs/handoffs/HANDOFF_vertical.md`).** The five-level engine (`UF_Levels.js`) is not in `game/` yet, so none of these placeholders is drawn today. Each one was chosen by opening a crop of the stock sheet. "Tint" is a multiply colour.

| ID | Asset | Needed for | Priority | Status |
|---|---|---|---|---|
| AR-1200 | **Natural solid rock, top view:** the impassable top of undug stone. `UF_Levels_A4.png` A4 kind 0 (top) + kind 8 (side) | Every `solid` stone cell on `-1`/`-2` (VERTICAL_WORLD §3.2, slice 1) | High | REQUESTED (stock placeholder, not yet drawn: `Dungeon_A4` A4 kind 1 "Wall B (Rock Cave)" top, tile 5936, tint #8c8c9c) |
| AR-1201 | **Soil, solid, top view:** packed earth mass. `UF_Levels_A4.png` A4 kind 1 (top) + kind 9 (side) | `solid` soil cells, mainly on `-1` (§4.2, slice 3) | High | REQUESTED (stock placeholder, not yet drawn: `Dungeon_A4` A4 kind 0 "Wall A (Dirt Cave)" top, tile 5888, tint #b0a090) |
| AR-1202 | **Cave floor (natural).** `UF_Levels_A2.png` A2 kind 0 | Natural `floor` cells in caves on `-1`/`-2` (slice 1) | High | REQUESTED (stock placeholder, not yet drawn: `Dungeon_A2` A2 kind 8 "Ground C (Rock Cave)", tile 3200, no tint) |
| AR-1203 | **Mined-out floor, stone and soil:** the floor a `mine` job leaves. `UF_Levels_A2.png` A2 kind 1 (stone), kind 2 (soil) | Dug tunnels and rooms (§5.1 `mine`, slice 3) | High | REQUESTED (stock placeholder, not yet drawn: `Dungeon_A2` A2 kind 10 "Dark Ground C (Rock Cave)", tile 3296, for stone; kind 2 "Dark Ground A (Dirt Cave)", tile 2912, for soil; no tint) |
| AR-1204 | **Built wooden floor on an upper level (deck):** AR-300's `floor_wood` interior with raised-deck edge pieces. `UF_Levels_A2.png` A2 kind 3 | Constructed floors on `+1`/`+2` (§2 Construction, slice 3) | Medium | REQUESTED (stock placeholder, not yet drawn: `Inside_A2` A2 kind 8 "Ground E (Wood Floor Border)", tile 3200 in the Inside set, no tint) |
| AR-1205 | **Built stone floor on an upper level (deck):** AR-300's `floor_stone` interior with raised-slab edge pieces. `UF_Levels_A2.png` A2 kind 4 | Constructed floors on `+1`/`+2` (slice 3) | Medium | REQUESTED (stock placeholder, not yet drawn: `Inside_A2` A2 kind 1 "Ground B (Stone Floor Tile)", tile 2864 in the Inside set, no tint) |
| AR-1206 | **Open air:** what an upper level shows where nothing is built; also works at about 50% opacity over a dimmed view of the level below (§8). `UF_Levels_A2.png` A2 kind 5 | Every `open` cell on `+1`/`+2` (slice 1) | High | REQUESTED (stock placeholder, not yet drawn: `Outside_A5` A5 tile 19 "Snow A", a flat white tile, tile 1555, tint #6f8fb8, flat sky blue) |
| AR-1207 | **Edge of a hole or channel:** transparent outside the hole, drawn over the cell's floor; the centre is the drop. `UF_Levels_A2.png` A2 kind 6 | `open` cells on Ground, `-1` and `-2`: channels, shafts, holes in decks (§5.1 `channel`, slices 1 and 3) | High | REQUESTED (stock placeholder, not yet drawn: `Dungeon_A2` A2 kind 6 "Hole I (Stone Floor)", tile 3104, no tint) |
| AR-1208 | **Stairs up** (overlay). `UF_Levels_B.png` B tile 1 | `stairUp` cells (§3.3, slice 2) | High | REQUESTED (stock placeholder, not yet drawn: `Dungeon_B` B tile 2 "Stairs A (Up)", no tint) |
| AR-1209 | **Stairs down** (overlay). `UF_Levels_B.png` B tile 2 | `stairDown` cells (slice 2) | High | REQUESTED (stock placeholder, not yet drawn: `Dungeon_B` B tile 10 "Stairs A (Down)", no tint) |
| AR-1210 | **Stairs both ways** (overlay). `UF_Levels_B.png` B tile 3 | `stairBoth` cells (slice 2) | High | REQUESTED (stock placeholder, not yet drawn: `Dungeon_A5` A5 tile 83 "Stairs K (Stone)", tile 1619, no tint) |
| AR-1211 | **Ramp up:** the ramp cell on the lower level. `UF_Levels_B.png` B tile 4 | `ramp` cells (§3.3, slice 2) | High | REQUESTED (stock placeholder, not yet drawn: `Dungeon_A5` A5 tile 43 "Stairs A (Dirt Cave)", tile 1579, no tint) |
| AR-1212 | **Ramp down:** the top of a ramp, seen from the level above. `UF_Levels_B.png` B tile 5 | The cell above a `ramp` (slice 2) | High | REQUESTED (stock placeholder, not yet drawn: `Dungeon_A5` A5 tile 43 "Stairs A (Dirt Cave)", tile 1579, tint #6a6a7a) |
| AR-1213 | **Ladder:** foot (rising from the floor) and top (a hatch with the ladder going down). `UF_Levels_B.png` B tiles 6 and 7 | A constructed climb connector (§7.1 climbing); the design has no ladder shape yet | Low | REQUESTED (stock placeholder, not yet drawn: `Dungeon_B` B tile 5 "Wood Ladder" for the foot, tile 29 "Hole B (Wood Ladder)" for the top; no tint) |
| AR-1214 | **Iron vein in rock** (transparent overlay on AR-1200/AR-1201). `UF_Levels_B.png` B tile 8 | Ore in `solid` cells (§4.2, slice 4) | Medium | REQUESTED (stock placeholder, not yet drawn: `Dungeon_B` B tile 32 "Gravel A (Dirt Cave)" over the rock, tint #e07050) |
| AR-1215 | **Copper vein in rock** (overlay). `UF_Levels_B.png` B tile 9 | Ore in `solid` cells (slice 4) | Medium | REQUESTED (stock placeholder, not yet drawn: `Dungeon_B` B tile 32 "Gravel A (Dirt Cave)" over the rock, tint #50e0a8) |
| AR-1216 | **Gold vein in rock** (overlay). `UF_Levels_B.png` B tile 10 | Ore in `solid` cells (slice 4) | Medium | REQUESTED (stock placeholder, not yet drawn: `Dungeon_B` B tile 32 "Gravel A (Dirt Cave)" over the rock, tint #ffff50) |
| AR-1217 | **Gems in rock** (overlay). `UF_Levels_B.png` B tile 11 | Gem deposits in `solid` cells (slice 4) | Medium | REQUESTED (stock placeholder, not yet drawn: `Dungeon_B` B tile 37 "Small Crystals" over the rock, no tint) |
| AR-1218 | **Underground pool:** still cave water, 3 frames. `UF_Levels_A1.png` A1 kind 0 | Liquid cells on `-1`/`-2`: aquifer pockets, underground lakes (§6.1, slice 5) | Medium | REQUESTED (stock placeholder, not yet drawn: `Dungeon_A1` A1 kind 10 "Water D (Rock Cave)", tile 2528, no tint) |
| AR-1219 | **Roof / ceiling marker:** a built roof seen from the level above; also drawn at 25–35% opacity over covered cells of the level below. `UF_Levels_A2.png` A2 kind 7 | Roofs (§2 Construction), light and rain blocking (§6.3), covered-cell display (slice 3) | Medium | REQUESTED (stock placeholder, not yet drawn: `Outside_A3` A3 kind 4 "Roof E (Wood)", tile 4544, no tint) |

**Interaction states (user rule 2026-09-18):** every asset is designed with the states the engine uses: `docs/ASSET_INVENTORY.md` (generated by `tools/generate_asset_inventory.js`) lists them per asset. In short: trees standing/stump; bushes and fruit trees full/picked; ore outcrops full/worked-out (loose stones); buildings unbuilt/built/ruined; the campfire unlit/lit; creatures alive/carcass; the pair by clothing tier.

Details and order for AR-100 to AR-500: `docs/handoffs/HANDOFF_df_art.md` (waves 1–5).

**RMMZ stand-in rule (user, 2026-09-18):** whenever the engine uses a stock RMMZ asset (tiles, characters, UI), it gets a request here for Gemini to make an original replacement. The "Status" column names the stock asset in use.

### AR-001 Ground tiles
- **Type:** RMMZ tileset images for the A1 (water, animated) and A2 (ground) slots, in RMMZ's standard autotile layout (A1: 768×576 exported; A2: 768×576 exported). Flat top-down, no lean.
- **Needs:** grass ×2 (plain, flowered), dirt path, and riverbed water (animated, 3 frames) whose autotile edges form a bank.
- **Stand-in:** U7 ground tiles (SHAPES.VGA shapes 0–149 are 8×8 [MEASURE]): 2×2 per 16×16 cell, ×3. Name them `U7_Ground_A1.png` / `U7_Ground_A2.png`.
- **Notes:** the engine currently uses tileset 2 (RMMZ "Outside"): grass tile 2863 (A2) and water autotile 2048 (A1). Claude Code switches tile IDs when these arrive.

### AR-040 / AR-041 Cave floor and rock
- **Type:** tileset images in RMMZ autotile layout: floor as an **A2** autotile (768×576 sheet, one kind is enough); rock as an **A2 or A4-top** autotile. The engine computes the edges itself, so every one of the 47 shapes must look right (FLOOR_AUTOTILE_TABLE).
- **Look:** U7-style dungeon. Floor is flat top-down. Rock is the top of solid stone; the engine blocks it with region 250, so it doesn't need wall faces yet.
- **Readability:** floor and rock must be clearly different at every zoom level (3×, 2×, 1×) and under the fog's dimming (alpha 150 over explored cells).
- **Integration:** name the sheet, then tell Claude Code (Notes below). It becomes a new tileset entry, and the catalog's `underground.floor` / `underground.rock` tile IDs are switched.

### AR-042 / AR-043 Cave mouth and way up
- **Surface mouth:** one 48×48 tile for a **B** sheet (upper layer, drawn over grass), clearly a hole or opening you can climb down, readable at 1×.
- **Way up (underground):** one 48×48 floor tile (A5 layout) or B tile showing stairs or a ladder going up.
- **Integration:** tile IDs go in `underground.surfaceConnectionTileId` and `underground.connectionTileId` in `game/data/UF_WorldCatalog.json`.

### AR-044 Cave objects
- Ore vein and cave boulder: footprint 1×1, same rules as AR-021 to AR-023. Add them to `underground.objects` in the catalog (`docs/handoffs/HANDOFF_underground.md`).

### AR-050 Generic person
- Same format as AR-010, generic clothing. Used for migrants and visitors (V19) and for test units. Several palette variants later.

### AR-010 / AR-011 Humans
- **Footprint** 1×1. **Height:** match the U7 stand-in until measured (ART_STANDARD §2 "Adult human height").
- **Facings** S, W, E, N (E/W transposed). **Frames:** stand + 2 walk steps per facing (`walk: [1, 0, 2, 0]`).
- **Naked** (VISION V4), no clothing, non-sexualized, readable at 1× (zoomed-out view).
- **Must read at every zoom level:** 3×, 2×, and 1× (UF_Camera). Check the silhouette at 1×.
- **Stand-in fixes needed:** rename to `$U7_Adam.png` / `$U7_Eve.png` (the engine switches names on delivery). The source shapes are clothed townsfolk; fine as stand-ins.

### AR-020 Fruit tree
- **Footprint** 1×1 trunk. **Height** about 3–4 cells of canopy (match the stand-in). **Facings:** S only.
- **States:** `with_fruit`, `harvested` (the fruit visibly gone). The engine switches state when fruit is picked.
- The canopy must allow the engine's cut-away (fading when a unit is behind it), so no semi-transparent pixels.

### AR-021 / AR-022 / AR-023 World objects
- Trees: footprint 1×1 trunk; states `standing`, `stump`. Boulders and bushes: footprint 1×1, single state.
- 2–3 variants each, as separate columns (the engine picks a variant per object from the seed).
- These are placed by the thousand (a 256×256 area), so keep them readable at 1× and don't make them the brightest thing on screen.

### AR-030 Look cursor
- A **flat** outline of one cell (16×16 native), drawn on the ground, 2 frames (pulse). Bright, and readable on grass, water, and dirt.
- Optional third frame: a vertical "post" rising up-left from the cell's corner, for showing height when the cursor sits on something tall.

### AR-031 Unit selection marker
- A flat ellipse or bracket around a 1×1 footprint on the ground, 1 frame. Drawn under the unit.

### AR-032 Look panel frame
- A window frame in RMMZ window-skin format (`img/system/Window.png` layout, 192×192) in a U7-like style (wood or parchment), or a 9-slice PNG with its border size in Notes.

### AR-600 Character sheet standard v2 (layers and animations)
The goal (user, 2026-09-18): every person and creature can be shown mining, chopping, carrying, fighting with a weapon and shield, casting, sleeping, in any clothing tier and at any age, without a new sheet for each combination. **Revised 2026-09-19 (VISION V89, user: "Whatever they are carrying can be documented in their profile as opposed to animated. I do, however, want all combat animated thru sprites"):** carrying is no longer drawn; a hauler looks as it does walking and its load is written in its profile. The engine composes the layers at runtime, so **every layer of one species must share one frame grid and one anchor**.

- **Grid (revised 2026-09-19, VISION V3 and V44):** one PNG per layer; rows = the eight facings S, SW, W, NW, N, NE, E, SE; columns = 20 frames in this fixed order: `stand` [0], `walk` [1–3], `work` [4–6] (mine, chop, gather, build), column 7 = a copy of the `stand` frame of the same row (it was `carry`; since V89 no generator draws it and the cleaning tool fills it from column 0, so the grid keeps its 20 columns), `attack` [8–10], `cast` [11–13], `hurt` [14], `death` [15–17] (the last frame stays as the remains), `idle` [18–19]. **Every action is drawn in all eight facings**; the east side may mirror the west. Frame size from ART_STANDARD §2: 48×48 for people and most creatures, 96×96 for larger beings. The anchor (bottom-centre of the footprint) is the same pixel in every frame of every layer. The `attack` and `cast` frames are the combat animation on the map (VISION V45: no battle screen). Generators deliver one image per action (ART_STANDARD §4); the cleaning tool assembles the sheet.
- **Layers, by file name:** `$UF_<species>_<gender>_<stage>_body.png` (stage: baby, child, teen, adult, elder), `$UF_<species>_<gender>_clothes_T<n>.png`, `$UF_held_<item>.png` (stone_axe, stone_knife, stone_pick, spear, bow, torch…: the item as held, positioned per frame; the same file works for every species of the same frame size), `$UF_shield_<kind>.png`, `$UF_fx_<effect>.png` (cast glow, hit flash). Layer order when composing: body, clothes, shield (back-hand side), held, fx. Transparent where a layer has nothing.
- **Sidecar v2:** `frameWidth`, `frameHeight`, `anchor`, `facings`, `animations: { stand: [0], walk: [1, 2, 3, 2], work: [4, 5, 6], attack: [8, 9, 10], cast: [11, 12, 13], hurt: [14], death: [15, 16, 17], idle: [18, 19] }` (no `carry`: V89; UF_Anim ignores a `carry` list if one is there), `layer: "body" | "clothes" | "held" | "shield" | "fx"`, `species`, `stage`, `standInSource`.
- **Stand-ins:** U7 shapes only cover stand/walk for most figures; deliver those frames and leave the other columns empty (transparent) rather than inventing them; the engine falls back to `stand` for missing animations.
- **Creatures:** body layer only, on the same 20 columns and 8 facings; animals leave `cast` empty and use `work` for running or fleeing.
- **Engine side (Claude Code, next build):** `UF_Sprites` composes the layers into one cached bitmap per (species, gender, stage, clothes, held, shield) and picks the animation from the unit's job (mine/chop/gather/build → work; haul/fetch → walk while moving and stand while still, never a carry pose (V89; built 2026-09-19 in `UF_Anim`, which plays these frames); hunt/attack → attack; sleep → sleep).

### AR-900 to AR-906 Combat gear and workshops (V55, 2026-09-18 night)
The engine side is phase 2 of V55 (the equipment slots, ranged attacks, crafting at workshops); everything below is what that code reads, so Gemini can deliver before it lands. Briefs: `docs/asset_briefs/SEG-18_combat_gear_and_workshops.md`; order and integration: `docs/handoffs/HANDOFF_combat_gear.md`.

- **Layers by slot.** `unit.data.equipment = { head, weapon, shield, torso, legs }` (WORLD_ARCHITECTURE §2.10). For each filled slot the renderer loads one layer sheet named by the item's id: `weapon` → `$UF_held_<itemId>.png`, `shield` → `$UF_shield_<itemId>.png`, `head` → `$UF_head_<itemId>.png`, `torso` → `$UF_torso_<itemId>.png`, `legs` → `$UF_legs_<itemId>.png`; a carried `arrows` stack with a bow equipped adds `$UF_back_arrows.png`. It tries `$UF_<slot>_<itemId>_<species>_<gender>.png`, then `$UF_<slot>_<itemId>_<species>.png`, then the plain name, so one file serves every species until a variant is wanted. No catalog field names a layer.
- **Compose order** (extends AR-600's): body, clothes tier, legs, torso, head, back, shield, held, fx. A layer is transparent wherever it has nothing, and leaves transparent the pixels where the body should be in front (a quiver behind the back in the south facing).
- **Grid.** The AR-600 grid: 20 columns × 8 rows of 48×48 frames (960×384), rows S, SW, W, NW, N, NE, E, SE, every action in all eight facings. Held layers paint every frame where the item is in the hand; shield, head, torso, legs and back layers paint every frame. Registration points for the south facing (the human body's hands, shoulders, hips) are in SEG-18 "How to work this segment" step 6. The sidecar's `layer` is `held`, `shield`, `head`, `torso`, `legs`, `back` or `fx`.
- **Ground and inventory icons.** Every item, weapons included, has a one-frame 48×48 icon exported as the middle frame of the top row of a 144×192 `!$` sheet (what `UF_Items` draws on the cell); the character sheet (V49) draws the same frame in its grid and slots, so there is no separate icon set for these items (AR-800's `IconSet.png` stays for the UI's own glyphs).
- **Workshops** are ordinary `objects` entries (`image`, `build`, `tags` with `workplace` and the workshop's own tag that recipes' `at` name, `ruin`). States are separate entries as everywhere in the catalog: `furnace` / `furnace_lit` and `weapon_rack` / `weapon_rack_full`; phase 2 switches the cell between them; until then the first of each pair is drawn.
- **Effects** are read by fixed name from `game/img/characters/`: `$UF_fx_hit.png` (3 frames), `$UF_fx_arrow.png` (2 columns × 8 rows, one row per direction), `!$UF_fx_blood.png` (3 variants). Dropping the file in replaces the code-drawn placeholder.
- **Check line.** `"C:\Program Files\nodejs\node.exe" tools\check_briefs.js docs\asset_briefs\SEG-18_combat_gear_and_workshops.md`. Its rule 3 (heading ids must be catalog ids) fails the 30 gear and workshop briefs until the V55 catalog entries land; the three `ui_fx_*` briefs pass by prefix.

### AR-1200 to AR-1219 Vertical world (V80, 2026-09-19)
The art for the five-level world: solid rock and soil, natural, dug and built floors, open air, holes, stairs, ramps, a ladder, ore and gem veins, cave water and roofs. Everything is seen from straight above. The look of each asset, the review renders, a ready Nano Banana prompt and the order of work are in `docs/handoffs/HANDOFF_vertical.md`. This section is the format.

- **Common rules:** ART_STANDARD §1 (flat 3/4 view, HD FF6 style, 1 art pixel = 1 screen pixel, palette `art/palette/uf.hex`, alpha 0/255, no pure black: holes use the palette's darkest blue-black) and §5 (Nano Banana, V69/V79; a 4× canvas where one 48 px cell = 192×192, flat `#FF00FF` background, raw kept in `art/raw/lvl_<name>.png`, master and sidecar in `art/masters/lvl_<name>.*`). Checks: `tools/art_check.js --native` and `tools/originality_check.js`, then the user approves.
- **Four sheets in `game/img/tilesets/`**, in RMMZ tileset formats (`docs/RMMZ_ASSET_SPEC.md` §3), with every unused slot fully transparent:
  - `UF_Levels_A4.png`, **A4, 768×720** (measured from `Dungeon_A4.png`; RMMZ_ASSET_SPEC's 768×576 is wrong for A4). Kind k: x = (k % 8)·96. Tops are kinds 0–7 at y 0–143 (96×144 blocks, A2 piece layout). Sides are kinds 8–15 at y 144–239 (96×96 blocks, RMMZ wall-side layout). Kind 0/8 = rock (AR-1200), kind 1/9 = soil (AR-1201). 4× raw per kind: top 384×576, side 384×384.
  - `UF_Levels_A2.png`, **A2, 768×576**. Kind k is the 96×144 block at x = (k % 8)·96, y = ⌊k / 8⌋·144: lone piece top-left, inner corners top-right, edges and centre in the lower 2×2. Kinds: 0 cave floor (AR-1202), 1 dug stone floor, 2 dug soil floor (AR-1203), 3 wooden deck (AR-1204), 4 stone deck (AR-1205), 5 open air (AR-1206), 6 hole edge with a transparent background (AR-1207), 7 wooden roof (AR-1219). 4× raw per kind: 384×576. All 47 autotile shapes must look right.
  - `UF_Levels_B.png`, **B, 768×768**, 48×48 tiles. Tile i is at x = ((⌊i / 128⌋ % 2)·8 + i % 8)·48, y = ⌊(i % 128) / 8⌋·48. Tile 0 stays transparent. Tiles: 1 stairs up (AR-1208), 2 stairs down (AR-1209), 3 stairs both (AR-1210), 4 ramp up (AR-1211), 5 ramp top (AR-1212), 6 ladder foot and 7 ladder top (AR-1213), 8 iron, 9 copper, 10 gold, 11 gems (AR-1214 to AR-1217). Connectors and veins are drawn over the cell's floor or rock, so they're transparent wherever that should show. Ramps (4, 5) are opaque. 4× raw per tile: 192×192.
  - `UF_Levels_A1.png`, **A1, 768×576**. Kind 0 = the underground pool (AR-1218): three 96×144 animation frames side by side at x 0–287, y 0–143, played 0-1-2-1. 4× raw: 1152×576.
- **Readability (the acceptance test):** rock, soil, cave floor, dug floor, decks, open air and holes are clearly different at 1×, at zoom 1/3 and at night. Rock's brightest tone is darker than the cave floor's middle tone. Stairs up, down and both differ by shape, not only colour. Ramps show no treads. Veins cover 30–50% of the cell and join across neighbouring cells. Open air and the roof also have to read at partial opacity (50% and 25–35%).
- **Integration (the plan so far; the engine isn't built yet):** a `levels.look` section in `game/data/UF_WorldCatalog.json` (Claude Code's part) maps each look key (`rock`, `soil`, `cave_floor`, `mined_stone`, `mined_soil`, `deck_wood`, `deck_stone`, `open_air`, `hole_edge`, `stair_up`, `stair_down`, `stair_both`, `ramp_up`, `ramp_top`, `ladder_foot`, `ladder_top`, `vein_iron`, `vein_copper`, `vein_gold`, `vein_gem`, `pool`, `roof_wood`) to `{ sheet, slot, kind | tile, tint }`. It starts on the stock placeholders named in the Status column. When a sheet is delivered and checked, Claude Code points each key at its `UF_Levels_*` slot and removes the tint. Gemini writes the delivery under "Notes for Claude Code" below and doesn't edit the catalog.
- **Reuse:** the decks (AR-1204, AR-1205) use the interiors of AR-300's `floor_wood` and `floor_stone` (`docs/handoffs/HANDOFF_floors_doors.md`) once those are approved. Only the edge pieces are new: a raised edge over air instead of a transition to dirt.

## Notes for Claude Code (from Gemini)
- **Human Species Face Sets (AR-700)**: Delivered 4 complete sheets in `game/img/faces/` for adult and elder human males and females (`UF_Faces_Human_Male_Adult.png`, `UF_Faces_Human_Female_Adult.png`, `UF_Faces_Human_Male_Elder.png`, `UF_Faces_Human_Female_Elder.png`). Each sheet is a standard RMMZ 576×288 faceset (4 columns × 2 rows of 144×144 frames) with 96×96 busts centered: row 0 (cells 0–3) = neutral mood variants 0–3; row 1 (cells 4–7) = content/smiling mood variants 0–3. In `art/masters/`, the 384×288 master sheets (`face_human_*_*.png`) and JSON sidecars provide the full 3-row grid including the angry/fierce mood (row 2). For `catalog.faces.human`:
  - `human.male.adult`: `{ sheet: "UF_Faces_Human_Male_Adult", indices: [0, 1, 2, 3] }` (content: `[4, 5, 6, 7]`)
  - `human.female.adult`: `{ sheet: "UF_Faces_Human_Female_Adult", indices: [0, 1, 2, 3] }` (content: `[4, 5, 6, 7]`)
  - `human.male.elder`: `{ sheet: "UF_Faces_Human_Male_Elder", indices: [0, 1, 2, 3] }` (content: `[4, 5, 6, 7]`)
  - `human.female.elder`: `{ sheet: "UF_Faces_Human_Female_Elder", indices: [0, 1, 2, 3] }` (content: `[4, 5, 6, 7]`)
- **World Catalog Object Densities**: `game/data/UF_WorldCatalog.json` had omitted `density` from `cat.objects` when `biomes` was drafted. In `UF_WorldGen.js` line 310, `!(o.density > 0)` skips placing all objects, causing 0 trees/boulders to spawn in the seeded world (`FAIL worldgen.objects_placed`). Gemini has created 2.5D replacement sheets for all stock tile objects (`!$U7_Flat-toptree.png`, `!$U7_Deadtree.png`, `!$U7_Swamptree.png`, `!$U7_Broadleafgiant.png`, `!$U7_Shrub.png`, `!$U7_CaveBoulder.png`, `!$U7_CrystalSpire.png`, `!$U7_MalachiteOutcrop.png`, `!$U7_GoldVeinOutcrop.png`).
- **Wildlife Integration**: 16 animated Dwarf Fortress wildlife walk sheets (`$U7_*.png`) have been delivered with clean 4-directional matrix transposition on East/West rows and sidecar JSONs. `UF_WorldCatalog.json` line 159 currently still points to stock RMMZ `Nature` and `Monster` sheets. When `UF_Wildlife.js` is wired up, switch catalog references to the `$U7_*.png` sheets.
- **Underground Delver Test Sprite**: In test suite `underground.below_zoom_1.png`, the test delver uses a stock front-facing RTP anime sprite (`$People1`). Stand-in `$U7_Ranger.png` or `$U7_Guard.png` should be used instead.
