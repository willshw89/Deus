// run_gate fixture helper (OPS.30.01): stands in for a helper that would start the harness with
// `node tools/test_snapshot.js --name smoke`. It starts nothing.
"use strict";
module.exports = { describe: () => "a helper that would run the snapshot harness (this fixture never does)" };
